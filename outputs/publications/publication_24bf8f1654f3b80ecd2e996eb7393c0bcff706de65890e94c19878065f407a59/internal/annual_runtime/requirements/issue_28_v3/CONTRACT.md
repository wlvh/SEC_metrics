# Issue #28 v3 — approved R4 label representation repair

This five-file successor preserves the exact issue_28_v2 parent and all its
business, source, geometry, period, unit, scope, ambiguity and call boundaries.
The sole additive policy is S-R4-LABEL-REPRESENTATION: after verifying the same
cell, accept exactly its stored raw_text or text and recover authoritative
raw_text for subsequent Evidence. Captions retain exact caption_raw_text.

The repository owner's approval is preserved verbatim as current-task policy
content in docs/evidence/issue_28_r4_label_policy.json. Its observed timestamp
is a capture timestamp, not a claimed GitHub publication time. This is not a
GitHub comment, transition activation, merge approval or live-call grant.

The normal controller acceptance, failure/success finalization, Run freeze and
disk replay select the policy from the record-bound Requirement. The request
binds that policy and states both accepted cell representations. Model output,
caller flags and environment switches cannot select a live policy. Historical
v2 records retain exact-raw semantics; original failed calls are not promoted.

The failure finalizer uses the existing legal FAIL check. Attempt/execution
receipts retain the error class; the general validation schema is unchanged.

Existing immutable SEC sources, task/Spec definitions and financial semantics
are unchanged. New source certificates are written only under docs/r4_v3/.
No source reacquisition, prompt exploration, benchmark rerun or broad audit is
authorized by this revision. Recorded fixtures and historical raw responses
used in offline regression confer no live qualification/publication credit.

The revision remains NOT_ACTIVATED until the existing exact-head transition
process completes. A new clean-head CLI plan and a new owner live grant must
precede any provider call. The intended shape remains nine base plus three
stability calls, retry=0, reuse=false, SEC=false, publication=false. The first
historical failed paid invocation remains in cumulative accounting. Active R3
is unchanged pending actual qualification and separate release authorization.
