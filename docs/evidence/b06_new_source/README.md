# B06 新来源：两份真实原生候选

两份未登记债务关系的真实申报均由确定性提议、当前原件关系验证及独立披露检查进入
既有 Calculator / Observation / Trace / Result，最终原生 Run 为 FROZEN/PASSED。
同一最终业务实现：`3e4ceff32fc09a245a078e867ac9a129a89f4d2f`。未使用模型响应。
新增真实业务调用 **provider 0 / paid 0 / SEC 2 / UNKNOWN 0**，两次SEC均HTTP200、零重试。
预算上限仍2/2/4；获取阶段已关闭。无合并、B06正式采纳、生产激活或active切换。

| 材料 | accession | 实际期末 / 财年 | 债务 / 归母权益（百万USD） | 原生比值 |
|---|---|---|---|---|
| Southwest | 0000092380-25-000024 | 2024-12-31 / FY2024 | 6699 / 10350 | 0.6472463768115942028985507246 |
| Salesforce | 0001108524-25-000006 | 2025-01-31 / FY2025 | 9111 / 61173 | 0.1489382570741994016968270315 |

Southwest：借款表组成含91融资租赁，6704减5发行成本=6699；流动1630＋非流动5069
对账。Salesforce：借款8433＋分列融资租赁678=9111，租赁流动337＋非流动341，
未折现付款708减30利息=678。独立原文/ET/算术核对见review/material-review。
这些是原始提交口径候选，不能描述为今天最新口径已验证；修订状态封存在各binding。

## 首次与修后表现

| 实现 | 实际结果 | 后续处理 |
|---|---|---|
| 88a56bd | 两份均FAILED：继承政策原件未完整复制，未进入计算验收 | 补齐父级执行证据文件 |
| 99e4b2c | Southwest重验FAILED：submissions SourceReference缺少目标accession | 将清单来源绑定到目标申报；保留原证明中的实际获取accession |
| 00ca4a3 | 两份完成关系与计算，冻结失败：内存tuple与JSON list比较不同；两份Run保持OPEN | 统一JSON可往返表示；未修改来源、公式或金额 |
| 3e4ceff | 两份正常Run均FROZEN/PASSED；异进程禁网冷读通过 | 同输入重入零新增调用；旧失败不改写 |

原始确定性提议随每次`.start.json`和binding保留，没有旧响应换绑或重调模型。
这些已被检查过的历史材料不声称严格盲测。结果内容无人工答案输入或逐份关系条目。

## 接受依据与界限

- 公式候选不继承coverage信用：从当前原件验证表内成员、融资租赁归属、流动拆分、
  调整对象及期末现值，然后结合独立披露清单推导覆盖。
- 清单先于选模型建立，资产负债表逐行检查；借款/租赁及相关表、未过滤的当前
  standard/custom融资事实另行归类。未知融资项目、缺页/续接缺失、原件冲突留缺口。
- 可信导入按固定受审main的获取记录及原字节；新获取通过原SecHttpClient、预写INTENT
  与受信执行日志登记。数据目录自造正文、headers和ledger不能自助登记。
- 两个HTML在首次Run前均不在Git对象库；执行前记录和独立祖先对象核对分别保留。
  source-checkpoint是执行后封存，不是首次原件Git准入条件。
- 支持有限债务/租赁披露语法，不证明任意自然语言。当前PR42 Southwest另外出现的
  24m供应商融资项目性质未证，新路径保持UNRESOLVED；旧v3结果/原规则不改。
  相应inclusive关系正例中的零余额派生材料明确TEST_ONLY，不计真实样本成功。

## 验证

- 本地fast：48入口、182项实际测试PASS，新增13项确实执行。
- Python 3.9：新增13项PASS。
- 原生材料：6项PASS，包括两个真实Run、来源准入进入冷读、旧关系/自然年入口不可用
  仍能读取、零调用重入、重新绑定伪验证记录仍被重算拒绝及正式根输出拒绝。
- 独立审阅保留初版4项错误接受、修复中过拒绝、金额符号冲突及最终关闭记录。
- 关键语义反例先合法重建TEST_ONLY身份，再断言实际关系/遗漏/金额检查层拒绝。
- CI及便携包最终核对见交付索引；本地测试不替代实际CI，原生候选不替代生产发布。

读取：`tools/vnext_b06_new_source.py read --data-root <外部data> --run-dir <Run> --output-json <新JSON>`。
`replay-index.json`映射完整冷读材料；只有新增字节存于objects，原仓库字节复用原路径。
精简包以内容哈希去重，materialize.py恢复code/cases后可在无Git、禁网条件下读取。

交付补充：真实正式版本及旧B06 v1/v2/v3完整候选通过只读PublicationView/原生读取。
便携包首次遗漏旧engine从code-root读取的政策捕获，补齐归档后两Run无Git/禁网冷读通过；
没有改变业务实现。首次CI新13项合并入口超过30秒，现拆成13个入口保持同一上限与全部断言。
