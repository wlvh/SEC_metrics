# 用户正式执行委托：B06 新来源接入，持续推进到集中交付

**委托来源：用户在 2026-09-11 17:05（UTC+8）明确要求“给codex下达指令，让他持续工作”。本评论由 ChatGPT 按该委托代为登记，将已讨论的 B06 新来源工作包及六处补充转为执行指令。它不表示用户已经审阅未来提交，不是 GitHub 代码 APPROVE，不授予合并或生产权限。**

**Codex：立即开始本工作包，在已授权范围内连续完成调查、实现、独立审阅、局部修复、真实材料验证、回归、归档、commit/push 和新 Draft PR。不要只回复“已理解、等待授权”，也不要在预检、设计、一次测试、提交或开 PR 后停下来请求继续。**

## 1. 起点与范围

仓库 `wlvh/SEC_metrics`，原目录 `/Users/lyuhongwang/Developer/SEC_metrics`，参考基线为 PR42 merge `92fdd0e30c5f05db9d3d0136424934037dcff66e`。先真实 fetch 并检查原目录、当前分支、工作区和远端；main 如有正常推进，按实际差异处理，不强制回退。

实时读取本 Issue 第1节三个最终目标、第3节当前队列、第4节剩余责任、第5—6节原则与权限。PR42已经收口，不重开、不重审全部历史。此次只推进 B06 的两类新来源接入：Southwest 代表“债务总额已经包含融资租赁”，Salesforce 代表“借款与融资租赁分别列示、证明不重叠后补加”。公司名只选验收材料，不能决定计算公式或赋予覆盖信用。

业务定义继续采用已批准的目标主体范围内已确认借款、债券及融资租赁负债的期末账面合计，除以同范围归母股东权益；融资租赁及已合并确认的非租赁组分纳入一次，经营租赁排除，禁止重复及猜数。Pfizer/JPM/Ford 的已有缺口保留，本轮不攻关这三家，不开发其他指标、不重启R4、不重做Marriott连续更新试点。

**交付目标：两份未登记债务关系的真实历史申报，在同一最终实现、同一业务定义和验证规则下，经正常输入入口自动形成关系提议、逐项验证、原生计算、验收及封存重放。运行不依赖逐份人工关系条目、答案、固定单元格或来源通过白名单。**

## 2. 连续执行权限和资源

本轮新增业务调用硬上限：**provider≤2、paid≤2、SEC≤4**。模型每份申报最多一次，两个名额分别属于两家公司，不转移、不重抽失败样本。允许零模型调用；沿用已批准模型和通道，不擅自换模型。进入运行链的额外模型复核同样计入上限。独立代码/验收审阅子任务可用，但其人工或模型结论不得充当运行输入、关系答案或生产许可。

SEC上限包括清单、Company Facts、年报、XML、附属文件等实际需要的业务请求；先复用已有可信材料，零自动重试。失败照计，UNKNOWN或计数不确定不得写成零，不借历史阶段余额。GitHub读写和CI另类记账。

允许现有机制内必要的B06专用适配、规则/合同版本化、受限试运行许可接线和真实委托登记；不得伪造签名、人工审核或GitHub APPROVE。不要把本评论当作能绕过运行权限检查的开关，应沿既有批准和运行控制机制落实本轮有限许可。

**不授新PR合并、生产规则激活、B06正式采纳、部署、active切换、回退/恢复写操作或长期任务。PR42的条件合并许可不延续到本PR。**

## 3. 材料与入口预检：内部完成，不另等批准

各取PR42所用申报之前最近的一份完整年度10-K，且其原件身份未登记现有B06债务关系。根据真实submissions和原件确认，不凭文件名选期。运行前固定样本；失败不换成容易通过的年度。独立参考答案只用于验收，不进入提议/验证/计算输入。允许检查资料和支持模式的可行性，不把两份已看材料夸称严格盲测。

在首笔模型调用前形成一张小表：两份accession、实际期末、财年标签、原始申报及修订；已有可信原件、缺少的具体文件和预计SEC请求数；模型可见输入和程序完整检查范围；关系模式的具体接受条件；实际创建/验收Run入口；实现、规则、提示和阶段许可身份。四次请求能否覆盖材料依赖必须先核清，不能浪费模型名额在缺资料的输入上。

使用原始字节和单独标记的模拟历史可见范围选择历史申报，不改SEC原始清单，不倒退正式结果。保持Salesforce非自然年度、`fy`、`reportDate`、目标accession、原始提交口径及修订影响的区别；不要直接套用仅支持1231财年末的旧年度入口或删除其原限制。当前Company Facts与历史原件分别绑定，不借其他accession的比较数字。

历史tag、布局或对账形式不同，可在本轮业务定义内实现一般适配；超出支持模式则如实记录，不能按公司强套当前公式。

## 4. 核心接线：来源、披露范围、关系验证分别成立

### 4.1 来源准入在实际Run入口生效

先列出实际经过的获取/导入、选择、创建、验收冻结和冷重放调用关系。`_verify_saved_input_origin()`位于完整候选封包模块；本轮不经过它，不能只修改该函数或绕过它来宣称新来源可信。

复用既有获取和导入机制，分别处理：既有可信保存材料（原获取记录与不可变字节）；本轮新获取SEC原件（获准入口建立URL、申报、返回字节和执行记录绑定）；合成/语义变造材料（明确测试身份，不获真实来源信用）。说明哪些执行记录被信任及其依据；调用者自造正文、headers、ledger且哈希自洽不能获得真实来源身份。不新建签名平台，不宣称防御控制全部受信代码与记录的操作者。

两份均须“关系未登记”；另至少一例正常Run需使用运行前未进入受审Git来源清单的外置必要原件，并证明接受不依赖其他历史Git路径对同一字节的预认可。简单复制已认可文件到外部目录不算。此项单独验收，预算内不能取得就如实保留缺口，不用关系未登记顶替。

### 4.2 检查范围独立于模型和计算白名单

区分允许计算的事实与检查遗漏的披露集合。后者来自本轮声明支持的完整资产负债表、借款/租赁附注及必要关联内容，包含自定义概念和表格行，不限于`concepts()`、`approved_concepts`过滤后的Company Facts。

先形成待解释融资项目清单，再逐项记录独立纳入、已包含、有依据排除、明确零/不存在或未解决。模型可以建议定位，不能自行裁剪检查范围。新增疑似融资项目、相关内容解析截断、必要脚注不可读不能被静默忽略。三个类别标签齐全不等于实际项目完整，未找到标签不能推出零。

### 4.3 选择计算模型不授予覆盖信用

`model_id`只提供公式候选；注册表中的`coverage`、包含和排除关系属于待证前提。目标主体、`scope_class`和必需类别由固定规则确定，模型不能缩小它们。验证器须从当前原件逐项证明前提，才可产生已验证覆盖与计算输入；不能经“选模型→继承coverage→完整”放行。

第一笔模型调用前，用两组实现规则和测试说明接受逻辑，无需新增长设计文件：

- 已含租赁型：组成行与总额的结构、融资租赁为何包含、current是否已含、折溢价/成本调整作用于何处、独立短债是否另在集合外。
- 分列租赁型：借款表的实际覆盖、另列金额确属期末融资租赁负债、原文或结构为何证明不已计入借款、流动与非流动是否重复。

提议仅给候选事实定位、关系和证据位置，不允许`complete=true`/`approved=true`或最终金额赋予审批作用。程序从原件重读金额、context、单位、期间、主体、分组及脚注，验证关系后推导完整性。引文存在、关键词命中、算式相等、同名tag、第二模型同意均不能单独证明关系。

支持范围限本轮两类可执行披露模式。复杂语义超出能力则待复核，不扩成任意会计自然语言证明平台，也不放宽标准迎合两份成功。

## 5. 原生接线与重放

继续使用既有Observation、Calculator、Trace、Result、验收和读取接口。允许必要适配，不另造计算器、执行器或发布器。新路径不得调用旧B06生产/补数函数，也不得在自动判断失败后借人工关系表放行。

封存原始来源及准入记录、检查范围、原始提议或真实请求/响应、逐项关系验证、规则/实现身份和完整原生记录。冷重放从这些封存材料重新验证与计算，不联网、不读当前docs人工表、不要求目标申报今天仍最新。旧关系文件、历史Run、旧候选及原规则字节保持原义。

交付两个完整原生Run、原生验收及异进程冷读，不强制重建250坐标候选包，不把历史测试结果混进当前年度候选。受影响的完整候选、正式版本读取和生产拒绝边界做针对性回归；原始提交计算与修订影响状态分开保留。

## 6. 验收与必要反例

正向：两份未登记真实申报在同一最终实现下完成正常输入、自动提议、原件验证、原生结果、独立原文/算术核对、冷重放。两份都待复核不算目标完成。至少一例外置新准入正例单独成立。

反例既包括原件不变但提议错误，也包括语义变造后重新建立合法测试身份、重新解析的材料。断言具体拒绝原因和实际检查层；不能因旧哈希、格式或权限提前失败就声称测到语义。必要类型：

1. 已包含租赁再加、含current的总额再加current、重复角色或公式。
2. 数值和算术不变，但包含租赁原文变成明确排除/否定，旧关系拒绝。
3. 在检查范围加入计算白名单外独立融资概念/表格行，原公式和类别标签仍全，完整性仍拒绝。
4. 使用权资产、未折现付款、错年度/主体/分组冒充目标负债；未找到或删除标签冒充零。
5. 自造匹配正文/headers/ledger冒充真实来源；伪造完整性、清空缺口或复用不匹配的旧验证结果。

动态重算正例：保持范围、单位、期间、关系和对账一致，成组改变原始金额并建立新测试身份，必须重读事实并算出对应变化的结果；不计真实申报成功。

保留旧包可读、实际active不变、新入口不依赖旧语义函数、同输入重入/冷读零新增调用、错误/UNKNOWN不前移成功引用。独立审阅对真实源文、关系适用和关键错误接受作集中检查；修复后审查实际变化。

## 7. 失败与持续推进规则

先以PR42已审材料完成主要开发和离线验证，再固定实现及规则执行两份验收。保留首次表现与最终修后表现。不得在首次失败后换样本、填答案或写文件特例追求通过。

两家公司无前驱依赖：第一份内容被正确拒绝后，来源、权限、模型身份、计数和共同验证器仍可信时，继续第二份独立案例；不转移名额、不重抽。来源真实性、权限、模型身份、计数UNKNOWN或共同验证器可能错误接受等系统性问题，暂停受影响的真实动作，先离线诊断。

范围内普通代码、解析、测试、接线问题自行修复并继续；不要每次询问“是否修复/是否继续”。额度耗尽只停止需要新增额度的动作，其他允许的离线修复、测试、审阅、归档继续。出现必须改变业务定义、扩大权限/架构或无剩余合法推进路径的阻断时，集中交付事实与最小所需决定，不无限循环。

验证器或原生接线修复后，可以用原请求/原响应形成明确的新重验；原调用身份和首次结果不可改。提示、模型可见输入或输出合同变化后，旧响应不能换绑为从未发送的新请求返回。看过验收材料再修复的最终通过不称首次盲验，保存实际差异。没有新增额度时保留新请求未验证的缺口。

## 8. 集中交付与停止条件

在原目录从核实main建一个新任务分支，不新建开发worktree、不弹stash、不强制清理现场、不在main混入开发提交。调查、实现、独立审阅、修复、测试和证据归档连续进行；简短进度为告知，不是新的审批关卡。

完成时交付一个新Draft PR及精简审阅包：材料身份与预检、支持模式、首次/最终结果、实际代码差异、原件各一份、原始提议/请求响应、关系验证、原生Run、关键反例、独立审阅、实际CI日志、调用账、大小和哈希索引。保留必需原件，避免嵌套前驱、重复源码或全环境。CI报告入口数与实际测试数，检查新测试确实执行；docs-only归档以差异绑定，不机械重跑全部历史。

开始执行时原位更新Issue #28第3/4/7节：登记本委托链接、状态为执行中、预算、当前停点；把“需新委托”改成“本轮已获委托”，保留PR42历史收口、三个最终目标与全部剩余责任。交付时再次同步真实结果，只有实际开始后才写已执行。

**最终状态：原目录新任务分支干净、已提交并推送、新Draft PR待集中审核；实际active仍为`publication_24bf8f1654f3b80ecd2e996eb7393c0bcff706de65890e94c19878065f407a59`，正式24指标不变，未激活生产权限，未合并、未启动下一指标。**

两份真实正例、独立新准入、关键语义拒绝、可重放、兼容和资源身份同时满足即收口，不继续加样本、加模式或打磨无关异常。未全部满足时仍交付已完成工作和真实证据，明确属于披露不足、解析/关系/验收实现问题、预算或权限缺口。当前唯一需要用户集中处理的停点是这份交付的最终审核或确实超出本委托的决定。
