# D04 offline source and response protocol

This is source preparation and recorded-response validation, not a model execution, semantic qualification or native D04 Run. V14 remains the unchanged 41a74984/327-file draft; these development modules are not enabled as a metric route.

- The actual census covers ten companies, twelve filings and 28,415 native facts. First packing preserved three failures where intact continuation objects exceeded 150k bytes. The separate 300k single-object bound resolved them; no source was truncated.
- V2 serialized all ten companies into 176 request drafts. V3 preserves all facts/objects while retaining nested XML once and checking every contained original slice: 161 drafts and 59,821,661 source payload bytes. Byte limits and draft counts do not prove provider token fit or model reliability.
- Complete source packets and drafts remain at the exact local paths in large-local-artifacts.sha256.json. Every gzip was decompressed and its compressed/uncompressed bytes were hashed. The small example request is only an inspectable draft, not a sent provider request.
- The current request manifest was rebuilt from the actual V3 source snapshots after re-verifying their source code/policy hashes and all original source proof bytes. Source trees were not re-parsed in that request-only rebuild. It adds the current fiscal-label context, retaining Salesforce's original metadata label beside its source-defined year.
- Default Python: current thirteen protocol tests PASS 58.153s; current Python3.9 thirteen tests PASS 76.304s. Earlier default thirteen-test run failed only because its ambiguous-quote test still used whitespace after whitespace was explicitly rejected; the test now uses a repeated nonblank phrase. Earlier protocol versions and Python3.9 receipts retain their own meanings.
- Recorded response tests use actual Enphase supplier/partner language. Complete replies still yield an unqualified proposal with provider execution, semantic correctness and native result flags false. Missing units/candidates, incorrect quotations/indices/types, scope conflicts and inconsistent current classifications are rejected or remain unresolved. Global unresolved reasons are retained; model-supplied offsets are unnecessary because the host resolves unique exact text.

Original 3d71577 GitHub CI completed all four jobs. Its log and terminal JSON are retained here; this gives no CI credit to the later R6 prototype. No provider/paid/SEC calls occurred. Independent review and authorized real-model feasibility checks remain pending. The remaining metrics, source update, full390 integration and production/retirement responsibilities remain open.

Reproduce using the APIs in docs/r6_interpretation_protocol.md, with repo_root set to the checked-out repository and an output directory outside it. The archived driver scripts preserve the actual original paths/commands for provenance; those paths may be changed for a new independent replay. Source and request JSON are serializable API outputs, not hand-maintained answer files. A fresh response-set inspection always rebuilds source inputs via inspect_recorded_responses.
