# A01/A02/B12 ordinary native source records and CI tiers

The new ordinary policy preserves the frozen deterministic catalog while expressing the actual result instant and canonical ratio/USD units. It reads declared units, official standard namespaces, catalog-required dimension QNames, and the source's extension binding. It is source preparation/native-record code, not a new frozen Requirement or Run.

Actual ten-company output is30 coordinates:3 numeric (JPM A01=.155, A02=.146; Salesforce B12=72,400,000,000 USD) and27 structural. Current subsidiary/advanced-method ratios and the acquired entity's separate RPO do not become the requested value. B12 is explicitly the RPO substitute, not ARR or churn. Salesforce's raw fiscal-year label conflict remains in prepared input; its actual measurement instant is2026-01-31.

- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=scripts python3 -m unittest tests.vnext.test_normal_accession_results`:6PASS/27.533s.
- Same command using `/usr/bin/python3`:6PASS/44.509s.
- `PYTHONDONTWRITEBYTECODE=1 python3 /tmp/sec_metrics_issue28_continuous/ordinary-accession-material.py --repo /Users/lyuhongwang/Developer/SEC_metrics --output /tmp/sec_metrics_issue28_continuous/ordinary-accession-material-final`:10 native material graphs; JPM/Salesforce each saved to JSON and rebuilt with a new interpreter against no-Git data roots. Exact script/index/two full examples and child logs are included. Source/policy/ledger hashes remain unchanged.
- Tests retain genuine full-source controls: unitRef renaming preserves meaning, false unit definitions reject, wrong entity scheme and malformed numeric grouping reject; rehashed changed result periods fail full source replay. Synthetic rebound source tests explicitly grant no acquisition credit.
- First census and first native graph logs are retained separately. The latter preceded replacement of the old raw-QName selector with direct native Observation/Calculator projection and is not the final graph identity.

## Actual CI failure and revised test classification

GitHub run https://github.com/wlvh/SEC_metrics/actions/runs/34672328421 on38a5d38 failed after533.059s: three LCR source selectors and one A13 selector exceeded30s. LCR already performs one full original-source evaluation per test, and multiple other full-source checks took26–29s. This was not repaired by deleting assertions, trimming input or calling a timeout a PASS.

The current workflow now runs two independently visible jobs. `--suite fast` contains95 short selectors with the existing30-second limit. `--suite source-material` contains32 complete source-material selectors with240 seconds per selector and20 minutes for the job. The coverage receipt proves every previous124 selector remains in the union, none overlap, and only the three ordinary native-material modules were added. Frozen V13 runner bytes and historical acceptance commands remain unchanged.

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/run_fast_tests_v2.py --suite fast --jobs 2`:95PASS/69.310s.
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/run_fast_tests_v2.py --suite source-material --jobs 2`:32PASS/171.269s.

The new source-material tier is not FAST_LOCAL_ONLY, live, full acceptance or evidence that the original30-second case passed. Actual GitHub results for the next commit must be observed independently. The new jobs run all source assertions, including the normal22 components; this does not freeze or publish their outputs.

A separate inventory checked414 actual source-body/header paths used by the previous ordinary material graphs: all are tracked in Git. It is not a fresh-source claim. New provider/paid/SEC calls remain0/0/0. Full39 integration, fiscal-label/subject/amendment repairs, independent review, complete390 publication/update/rollback/retirement and final production confirmation remain unfinished.
