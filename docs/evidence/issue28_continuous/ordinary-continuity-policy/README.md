# 普通接续主体的既有不可比规则接线

本增量使Paramount B02/B04/B05/B07沿既有批准目录的REQUIRE_CONTINUOUS规则，输出NOT_MEANINGFUL / ENTITY_CONTINUITY_NOT_COMPARABLE及空值。旧入口在到达原计算器守卫之前就报“SUCCESSOR_SCOPE_NOT_IMPLEMENTED”，混淆了规则限制和开发缺口。本次没有修改冻结目录、指标含义或把结果改成N/A，也没有拼接前后主体金额。B08/B09继续是同当前主体、同一期末的EXACT数值。

原年报、修订及不能证明财务值范围的限制均保留。主体不可比判定只使用已经核对的登记主体、原件身份/期间和现行目录，不需要财务数值。任何数据源真实性失败仍拒绝；错误的守卫返回不能留下伪造数值或观察记录。

首次真实六坐标原生运行中，前四项被通用Run检查拒绝：“NOT_MEANINGFUL Result lacks evaluated inputs”；B08/B09已通过。原始材料完整保留。修复只允许一种窄分支：已经从原件/登记/目录完整重算的ordinary_case，且实际结果与其确切预期一致、理由/Trace均为主体不可比、无数值和数量输入。其他NOT_MEANINGFUL数量要求不放宽。

修后六个实际OPEN Run和公共行全部形成，运行2026-09-13T08:52:11至08:59:40 UTC。来源层10项测试覆盖十公司×11项记录、原反例和守卫反例，PASS66.657秒；100组fast PASS79.61秒。重新签名、结构合法的“无关空输入理由”和“伪造数值”两组记录均被完整来源图重算拒绝；复制包的Python3.9无Git/无网络冷读B02通过。语义、公司字面量与出口检查通过。

native-continuity-material.tar.gz与archive-members.json保存原失败、修后六坐标、两项攻击、源检查日志及新调用离线接线，每个成员均按SHA与大小读回。offline-wiring.json重新绑定当前V15；旧a46608d与780d9ba包、634/e1ac快照及审阅范围不改。此次仍是真实保存原件上的开发验证，provider/paid/SEC=0/0/0，不是新SEC获取或正式发布。

同时只读核对了来源依赖：JPM有6个历史分片与索引声明日期范围不一致，需真实刷新清单及相关分片，不能给旧响应换新请求身份。Salesforce缺少/失败的是2025原年报HTML及一份8-K头文件，确切URL在包内jpm-salesforce-current-source-dependencies.json。发现来源依赖的CLI exit2是预期未决状态，不是已获取或完整39来源验收。

当前模型密钥仍缺，真实模型可行性只受该环境依赖影响；安全离线开发继续。后续需要有限SEC真实获取接线、D03/D04核心语义、B13完整自动结果、JPM/Ford等剩余范围和缺证、完整390、普通更新/发布回退/旧入口退出，以及未覆盖模块的独立审阅。旧Codex审阅子任务9月17日前不重试。无Ready、合并、正式采纳、部署、active切换或长期运行权限。

CI接线复用同一个当前余额作业：构建六项Run后，原现金伪造/修订缺失检查与新连续性终态重签检查分别执行，保留原20分钟作业限制。a46608d的CI34747654287十项作业最终全部SUCCESS；它只证明前一提交，不替代当前变化的新CI。
