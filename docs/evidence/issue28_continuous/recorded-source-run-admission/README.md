# Recorded source admission into ordinary Runs

Read validation-summary.json. The final346-file UNFROZEN runtime admits three owned recorded request rows through the ordinary fiscal/zero-AI/Company-Facts/lodging routes. Five OPEN Runs contain six results including B03's B01 dependency. Five changed-graph/history cases reject. Public rows/receipts label recorded source credit. B03 and B10 copied-runtime cold replay, plus B03 Python3.9 cold replay, pass without Git or network. The unchanged-source comparison is NO_SOURCE_CONTENT_CHANGE.

The complete final candidate data/runtime, Runs, local execution journal and altered controls are in native-material.tar.gz; every archived member was read against its SHA256 and size. Full indexes also retain duplicate input/attack copies. installed-owner-registration.json is the exact installed private journal entry, independently compared to the exported checkpoint. This follows the existing installed-code/execution-history trust boundary; data alone cannot enroll a ledger, but the mechanism does not defend against an operator replacing both trusted runtime and its history.

Initial failures remain separate: a source test expected the wrong exception when a reused original body was altered; the first material used a /tmp alias and stopped before any recorded session; the first cross-route CLI passed B03/B08/B09 but failed B10/B11 because their nested source reader still used the old verifier. The final fixture uses a resolved path and the corrected reader. Earlier roots are indexed rather than relabelled current; their original directories remain at the recorded paths. The corrected B10/B11 CLI succeeds and preserves recorded-source labels.

Default baseline native regression covers15 scenarios, including five original-input Runs and their attacks, with protected root state unchanged. Source45 and fast97 selectors pass. Previous5958234 CI completed allfivejobsSUCCESS; the new commit's CI is separate.

No new financial source bytes, live SEC fetch, provider call, quota reopening, production adoption or active switch occurred. Old frozen input wrappers still require explicit successor wiring; this is not all39/new-filing/continuous-runtime completion. Independent review and the complete Issue28 objective remain pending.

Earlier/default-regression file-tree indexes are losslessly compressed as .json.gz. compressed-indices.json retains original byte hashes/sizes and exact decompression verification. The current native archive index remains plain JSON.
