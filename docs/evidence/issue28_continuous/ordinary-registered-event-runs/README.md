# 完整前身来源获取与6事件原生结果

18个必要前身8K正文/头文件已逐笔真实获取成功，9对原件与原清单申报编号/日期核对通过。本轮批准后的provider/paid/SEC累计0/0/31，无失败、UNKNOWN或停用通道；旧13个slot不重写，新增18个slot的原请求/响应/执行规则/终态在本包，原13次材料仍在ordinary-document-identity。旧关闭额度不恢复，D-36及240/240/80不变。

## 实际结果与窗口

主/前身两CIK、35份完整申报、70条事实经既有目录/SourceSet/Claim/Calculator重建：

| 指标 | 结果 | 实际事件窗口 |
|---|---:|---|
| C01 | 12 | 2024-01-01至2025-12-31 |
| E01 | 5 | 同上 |
| E02 | 0 | 同上 |
| E03 | 12 | 同上 |
| E04 | 0 | 同上 |
| E05 | 4 | 同上 |

全部为原生OPEN候选及已生成公共行，不是生产发布。该窗口来自旧批准的PRIOR_CALENDAR_YEAR_START_TO_TARGET_END，并非把两年事件说成仅2025发生数。收入B01/EBITDA B03的财务主体限制保持，未将两主体财务拼接。自本轮来源补齐起累计23个独立正向坐标恢复，不代表390已通过。

第一轮把事件窗口直接用作Run坐标，被原53周限制拒绝；原6项失败及源码保存。修后Run保留一年报告坐标2025，事件观察/结果和来源集合保留其真实回溯窗口。run_store先独立重建完整原件case并逐条核对，再只给确切的6事件记录认可期间差别；records.py通用53周及其他财务期间检查没有放宽。

## 负例与验证

构造遗漏前身的结构合法原生E02=0（与正确值相同），恢复完整注册表规则后被ORDINARY_INTEGRATED_INPUT_BINDING_CHANGED拒绝。该反例不靠数字不同识别错误，证明闭合来源范围仍是条件。首次测试写错预期错误名，真实拒绝已发生，原错误日志与图保留；修正断言后材料测试PASS53.754s。

6项实际原生及公共行通过；Python3.9、独立复制运行时、禁网冷读C01共224记录通过，从审核包实际还原后再冷读通过。旧record schema6项通过，普通零AI旧9个方法通过，变更的C01缺源分类方法单独回归PASS56.719s，B01仍为原财务范围限制；首次命令误引用不存在的test_records模块及旧分类断言失败均保留。101快速检查PASS82.040s。当前工厂禁网PASS38.318s；按执行字节差异，SEC获取/准入/计数代码未改，复用128fe33的94.031s证据，不写成重跑。

当前后继闭包dbe6016d/371；新接线收据绑定当前执行，原31个调用各自保留原时点规则。当前读取批准/门控/总账复核通过，无新增调用。执行者自查和CI不属于独立审阅。

## CI实况

128fe33为9个job成功，source-material失败；此前current-instant/debt超时修复后的任务均成功。原source log证明只有完整SEC获取材料超过240秒（return124），其余来源检查通过。该单项含复制、三次模拟获取、原生Run及冷读，现仅给它480秒，其他来源测试仍240秒；整组job35分钟。超时仍返回124，所有选择项和断言保留，路由验证在registered-event-timeout-routing.log。未改变任何模型/SEC请求资源或自动重试规则。新提交CI仍须实际完成，不能用本地或较早head替代。

## 材料与恢复

3196个路径/860个唯一对象/65516954压缩字节，全部对象读回SHA/size核验，已有仓库引用同样核验。包含新18个真实slot、原期间失败/修后6Run、首错/修后负例、当前factory记录和日志。原首次缺前身的探查日志保留；探查的通用latest JSON刷新为完整范围后另存after-capture版本，不把该工作文件称作原失败的不可变快照。

```bash
REGISTERED_EVENT_NATIVE_BATCH=<六项原生目录> REGISTERED_EVENT_SOURCE_ROOT=<已验证来源根> REGISTERED_EVENT_ATTACK_ROOT=<新外部目录> PYTHONPATH=scripts python3 -m unittest tests.vnext.test_registered_event_run_material
python3 restore-document-material.py --evidence <本目录> --repository <PR43仓库> --output <新外部目录> --prefix registered-event-native-second
/usr/bin/python3 cold-live-checkpoint.py <新外部目录>/registered-event-native-second paramount_skydance_paramount_global C01
```

本地真实来源材料测试已执行；没有该来源根的CI不能把跳过写成新增覆盖。恢复程序不安装调用权限，不写全局总账或active。剩余财务接续、B13、D03/D04、390、正常更新、发布/故障回退恢复、旧入口退出及独立模块审阅继续承担。模型凭据缺失只影响真实模型请求；9月17日前不重试旧Codex审阅子任务。无Ready、合并、采纳、部署、active或长期运行许可。
