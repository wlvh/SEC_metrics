# Issue #28 v5: fixed-code annual candidate runtime

Adds S-ANNUAL-RUNTIME to the exact issue_28_v4 parent. Its policy content comes
from the current user stage instruction. Historical engines, grants and
Requirements retain their meaning; the PR36 open-PR grant remains consumed.
A new owner issue comment records the delegated, reviewed stage scope. No
active development PR or input Git commit is needed to process saved inputs.

Marriott B01 uses the existing structured Run, including its native attached
metrics. Only B10 uses the unchanged full-document occupancy task. Code and
all policy/Spec/task/schema files must match the reviewed runtime tree in a
clean checkout. The external data root holds exact verified authority copies
and immutable SEC attempts with the original ledger identities. Python is
always imported from the original checkout, never the data copy.

A stage fixes the reviewed code/policy, company, metrics, provider/model/API,
source rules, absolute stage/data roots and total budget: provider/paid <=1/1,
SEC=0, retry=0. Each input gets an exact plan automatically. A new year never
requires a new policy or fixture. A permanent exclusive stage slot prevents
new inputs, heads, directories or processes from resetting the allowance.
Existing WB-3 owns request reservation, egress, failures and receipts. Missing,
inconsistent or >200000 actual input usage is a failed post-execution gate;
no estimate is represented as actual usage. Any failure/UNKNOWN stops calls.

This stage explicitly uses an isolated historical successful candidate as its
comparison start; the current production publication is displayed separately.
Only both requested metrics succeeding advances the local successful reference.
All raw runs and failures remain. Reentry with unchanged annual identity makes
no provider request. Native Result PUBLISHED never changes formal active R3.
There is no qualification/publication credit, no new release bundle, database,
transaction system, background schedule or expansion to remaining metrics.
