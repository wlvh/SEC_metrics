{
  "status": "BINDING_REJECTED",
  "error": "Normal candidate rule bytes differ: scripts/vnext/normal_governance_input.py",
  "cause": "Existing metadata parser is frozen in retained V12 rule set; source-only optional argument still changes its bound bytes",
  "repair": "Only the two-file registration discovery patch reversed; author preparing explicit discovery helper in existing unfrozen module; frozen snapshots not changed",
  "real_calls": [
    0,
    0,
    0
  ]
}
