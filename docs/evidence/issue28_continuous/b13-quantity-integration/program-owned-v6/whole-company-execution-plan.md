# B13 whole-company V6 execution plan (not an execution receipt)

## Finite task and gates

Use the already-approved Ford/Enphase physical production / available capacity meaning, current explicit V6 source/request format and the existing Spec/Calculator/native Run. The planned maximum is 17 new provider and 17 paid requests, with zero new SEC requests for these saved complete originals. This is a component of the one Issue28 cumulative plan, not a separate grant or reservation. Before execution the parent must reconcile it with the fixed ledger and all remaining D04, D03, updates and final-acceptance work; 240/240/80 is never reset. B13 LIVE remains paused in the current staged code until review, current binding and wiring are satisfied.

## Source-backed sequence

1. Complete Enphase B13: six source requests covering all 29 units. Group1 has five units/five required candidates and the positively proved original quarterly capacity assertion; it also contains the previously mislabelled sales context. This is the first representative and belongs to the final company task. If accepted, retain its original receipt and continue groups2–6 under the same fixed contract. Largest input is186848 tokens (group3); group5 carries seven units. Each unit still must respond, including those without navigation candidates. No quarter-to-year arithmetic is allowed.
2. Complete Ford B13: eleven source requests covering all52 units. Group1 is the relationship/candidate representative (five units,16 required candidates,186406 input tokens) and directly replaces the defective V5 contract seen at original94 without retrying its identity. Group7 has13 candidates. Group10 carries eight units and193527 tokens and tests the near-limit all-unit response obligation; group9 also has eight units and one candidate. Complete all11 exactly once, retaining valid successful receipts. Zero positively proved physical quantities does not prove no disclosure; the model still must account for the original remaining candidate set and full units.

`company-task-request-plan.json` binds each exact request ID, provider-body SHA, unit IDs, candidate count/type, program proofs and constraints; every body SHA matches the final exact counting evidence. The plan considers input length, unit/candidate load and relationship complexity. No synthetic diagnostic batch or disposable real result is added.

## Completion and failure handling

For either complete company set: register only original successful receipts under current checks; install/replay the native Run; project the permitted text/numeric/defined-absence result with exact original evidence; feed that same registered set through ordinary per-metric update history, repeat triggering and cold historical reading. A real valid result may then satisfy that coordinate. Current source facts suggest Enphase may support reported quarterly capacity text with a real annual-pair limitation; Ford's final interpretation remains to be established. Neither expected outcome is preassigned to the model or claimed complete here.

200000 context,8MiB,120 seconds and zero automatic retries remain. Every known source-side unsupported quantity relation stops before provider execution as an implementation gap. A failed response remains failed: preserve raw input/output/receipt, diagnose its actual rejected content, and pause the affected route if the contract or validator needs repair. Do not rerun a failed identity, edit a prompt to redraw, relabel sales/shipments, infer missing production, or turn unknown syntax into a disclosure limit. Unaffected authorized work continues. Original68/82/94 and historical cancelled/failed calls retain their original status and current credit restrictions.

No Ready/merge/production adoption/active switch/deployment/long-running authority is created by this plan.
