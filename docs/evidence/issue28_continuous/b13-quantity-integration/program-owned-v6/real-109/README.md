# Original real B13 V6 attempt109: cross-unit reference rejection

Original109 remains FAILED_TERMINAL with B13_REFERENCE_OUTSIDE_SUPPLIED_SOURCE, no complete B13 result or usable business-result credit. Provider input184968/output778; fixed totals after the attempt60/60/49. The remaining five Enphase requests and all Ford V6 requests were not executed by this task.

All five requested units were present in the response. The supplied visible-block ranges were0–378,379–634,635–1000,1001–1410,1411–1883. The model put evidence834 under the second unit (379–634), though834 belongs only to the third unit (635–1000, also containing program-owned capacity809). Other references443,454,1022 were in their correct units.834 is a real original source_index, not a row offset: its reason matches the supplied statement about contract manufacturers having sufficient production capacity for anticipated demand. The original restored source contains the complete block; no loss or wrong original-source decoder mapping was observed.

The actual current validate_response rejects the original bytes with exactly the recorded reason. A separately saved synthetic diagnostic moved only finding834 to its uniquely supplied owner, keeping its label, subject, timing, evidence index, reason and all remaining response content unchanged. The actual validator and native build_acceptance then passed, including five total findings after the program-owned quantity was merged. That modified response is synthetic diagnostic evidence only; it does not repair or upgrade original109, establish a full Run, or prove all business interpretations correct.

The old V6 contract is explicitly nested: response_protocol.unit_fields includes findings and evidence belongs under that unit; capacity_semantic_review.py derives _source_items from row.unit_id then requires source_index membership. Native INDEXED_UNITS_V1 also explicitly retains WITHIN-that-unit semantics. The observed rejection enforces the original contract. No automatic reference relocation was implemented.

original-material.tar.gz preserves the complete original call slot: request/source, wire/assistant response, journal, terminal, native execution/reservation/egress receipts, and execution-rule archive. It additionally includes the complete original SEC bodies/headers named by source_proofs, recovered frozen runtime files, task failure output/log and point-in-time cumulative allowance/claims. manifest.json maps logical paths to exact SHA256 blobs. Full extraction and recovery of every logical file passed; the originals were rehashed afterward. One call has540 logical files and540 distinct blobs,32,126,270 uncompressed bytes; compressed archive10,568,718 bytes. No archival byte is used to upgrade failed business credit.

The pure zero-provider finish script is preserved for later complete successful sets; it was not executed for this incomplete failed set. Structural reference-contract alternatives are investigated separately, with no main runtime edits, prompt changes or new model calls.
