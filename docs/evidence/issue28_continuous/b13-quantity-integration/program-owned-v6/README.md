# B13 explicit program-owned quantity contract (V6)

This is author implementation evidence, not independent review or real company acceptance. Patch `4e9428ce69b4ab8298e35ac98116fa68bbc549afe20d3336fed2a7ba8b704b34` is staged outside the main runtime. The reviewed ability prerequisite is `463c91ee2f45cea5f25d0a43f1623ce72b5c0067f292c3638ac2999d5b9767ca`. `patch-state.json` binds all changed files.

## Why this changes the failed contract

Original real executions 82 and 94 remain failures. V5 let the model label sales, qualitative production discussion, or industry capacity as current physical production/capacity; contradictory limits could reject the answer, and later source-role repairs added independent rejection. V6 now derives only positively established physical quantity roles from original source using the reviewed bounded role checks. Those verified findings are merged before candidate coverage checks. The model handles remaining source interpretation and must still respond to every original source unit. It cannot emit ACTUAL_PRODUCTION or AVAILABLE_CAPACITY. No redundant acknowledgment is needed for a program-owned quantity reference.

This does not guess missing production, calculate from shipments, annualize a quarterly capacity, or let proof count zero establish absence. Visible quantities with unsupported syntax, relevant native fact concepts without a supported original role, and native supplement quantities remain explicit implementation gaps. Proven monetary concepts/units retain their existing nonphysical interpretation. The existing same-subject/period/product/facility Calculator independently decides whether a ratio is supported; empty model calculation_limits establish nothing.

V6 includes original-source quantity metadata in Candidate/Evidence and recomputes it during both numeric and text native result preparation. Its prompt describes the existing lossless shared contexts/units/namespaces, HTML style-reference restoration, and object-key table layout. Original V5 defaults, prompts, request identities, original receipts and historical exports remain unchanged. The current ordinary update and finite refresh entry points select V6 through one common function; the low-level factory default is still V5. B13 LIVE refresh remains paused.

## Completed evidence

- 32 focused source/role/protocol tests, then 14 V6/refresh routing tests passed. The latter exercise the actual refresh coordinator, native dispatch and ordinary configuration, with deliberate source/registration boundary doubles.
- Complete actual Enphase source, six new recorded responses under the latest V6 prompt: native OPEN Run and public row passed in 255.299 seconds. Copied package cold replay passed in 64.961 seconds. The row preserves the reported approximately five-million microinverters per quarter; it is text, not an annual utilization ratio.
- After the routing repair changed runtime identity, the same six original recorded receipts passed current-acceptor zero-call re-registration in 52.299 seconds. Every original ledger file remained byte-identical. Old Runs were not re-signed.
- Earlier prompt identities and the associated native/cold/attack records are retained separately, never passed off as latest-prompt execution.
- Exact provider-body counts: Ford 11 requests / 52 units; Enphase 6 / 29. Maximum input plus 4,096 output reserve is 197,623 and 190,944 respectively. Every request fits 200,000 tokens and 8 MiB. No extra compression iteration or real call was performed.
- Saved V5 requests 82/94 reconstruct byte-identically; neither failure nor original 68 is upgraded.

`local-material-files.json` records complete local material paths, byte sizes and SHA256 values. Small Run graphs and execution summaries are copied here; full original sources, recorded wires, receipts and installed packages remain at the listed local paths. These are real-source/recorded-response checks only: zero new provider/paid/SEC executions, zero real B13 coordinate completion, no production permission.

Independent review is separately authored at `../../b13-quantity-role-repair/program-v6-independent-review/`. It retains the original edbc refresh-contract mismatch and the exact 4e9428 follow-up. This author's report does not broaden that scope.

## CI commands

With the existing pinned tokenizer dependency installed and `PYTHONPATH=scripts`:

```sh
python3 -m unittest -v tests.vnext.test_capacity_program_roles
python3 -m unittest -v tests.vnext.test_capacity_program_run_material
```

Add the first module to existing fast tests. Run the material module in a separate step with a unique `B13_NATIVE_RUN_MATERIAL_ROOT` (for example `${RUNNER_TEMP}/ordinary-b13-v6-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}`). The existing V5 material test must keep a different output directory; both enforce immutable new directories. The V6 test explicitly selects reference context and program-owned roles and runs the unchanged complete-source native lifecycle plus signed-export attacks. Its exact unittest invocation passed in779.914 seconds under the current binding (`unittest-material-final.log`), including full Run/rows and three signed-export attacks. Use a separate capacity V6 material job with at least20 minutes, rather than appending this whole-source replay to the existing15-minute V5 job. This direct test deliberately exercises the ordinary unbatched replay; the earlier255.299-second script uses the already-existing request-construction session and remains separate evidence.

## Remaining responsibility

Parent integrates only the reviewed patch, refreshes current execution bindings and runs affected wiring. Then carry out the bounded whole-company V6 request plan using the fixed ledger and existing authorization; successful new requests retain their coordinate credit. Original V5 failures are not retried. New-contract calls require new original requests/receipts and cannot reuse V5 credit. Full B13 acceptance remains contingent on those real company results and native/normal-update replay.
