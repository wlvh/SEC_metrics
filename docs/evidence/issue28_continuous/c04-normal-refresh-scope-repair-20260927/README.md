# C04 刷新范围限定回修

`b2f7ed41`的限定独审在`c04-normal-refresh-20260926/independent-review/conclusion.md`记录两项P2，原结论保留。本次只回修相应范围：公开有限刷新CLI仅在选择集合含C04时打开C04后继，B01-only仍走旧入口；旧来源根的C04处理副本豁免只有**单独C04**请求可以用于SEC获取。若旧根存在这些历史处理配置且请求混合其他指标，先不申领任何SEC槽，保存来源的C04仍可尝试形成候选，其他指标明确为当前处理输入未满足，整次刷新保持未完成。新根或处理规则字节当前的混合请求仍走严格普通获取。仅缺新C04规格而其他规则当前，不被错判为旧根冲突。

最终绑定下`boundary-final.log`的10项短测通过，涵盖B01-only公开入口选择、缺规格边界、相邻结果失败不被掩盖与原SEC接线拒绝。`source-scope-final.log`的两项原件材料测试通过（70.002秒）：旧根混合B01+C04带两次请求上限但实际没有调用录制传输，C04形成候选、B01仍`UPDATE_BLOCKED`，总账测试槽零增长；单独C04模式可读取旧规则副本而不覆盖，其他规则字节变造仍拒绝。`c04-two-version-final.log`在当前代码与需求绑定下重新验证两份真实保存的Marriott申报清单、Company Facts和四次**录制**获取：两次正常刷新均得到C04候选且旧版可重读（201.091秒，真实调用0）。先前在长测运行期间修改执行文件造成的中断失败保留在`c04-two-version-regression.log`，不能登记为通过。

原真实账本已有保存来源中，Marriott当前发现的29个来源URL均被显式C04原生输入的29项来源证明覆盖；当前下一步可能刷新的是申报清单和Company Facts，均在此集合内，详见`c04-source-map.log`。这只证明**当前保存范围**的来源责任；收到新的申报清单后必须重新发现并核对新URL，不能凭这份列表给未来未知文件永久授权。`final-wiring-after-source-map.log`验证当前V14需求闭包、执行文件和三份零调用收据；其SEC authorizer在脚本中是替身，真实传输仍须原账本权限与前置条件。

普通V13需求身份保持`sha256:a573ed50d6a5e09e3477de53e5a8f83b01a600220379fdc8d73aba19bb32b3a2`；C04范围收紧后的连续V14身份及收据见`binding-summary.json`。原真实账本仍是143／143／49，B13 V6停用、D03无真实请求及生产边界不变。本修复没有使旧处理根上的B01等普通指标获得当前规则的完整运行能力；混合运行的扣留是公开缺口，不得把C04候选扩称全部正常更新成功。下一步先做精确新SHA限定审阅；若通过，按既有#28来源获取许可和原账本守卫，首个实际SEC获取最多申领一次，按返回的真实发现重计后续需要的URL与权限，不把旧32或账户余额当成自动许可。
