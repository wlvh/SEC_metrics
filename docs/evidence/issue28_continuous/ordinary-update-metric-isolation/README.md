# Independent ordinary metric updates

The original grouped-state probe is retained: actual Pfizer B01 was PUBLISHED while B06 was WITHHELD, but no successful group reference was stored (133.881seconds). The normal company entry now reuses the existing controller in separate metric state directories. Successful metrics keep independent verified candidates even if another metric is withheld, has a current input failure, or has a damaged historical output. No business meaning or withholding criterion changes.

The actual B01/B06/B08 material proves the mixed outcomes, repeated input without new Runs, failure of an earlier metric while a later metric continues, retained original periods with current_input_matches=false, restored input reuse, and selective rejection of changed successful CSV bytes. Original group histories are rejected by the new company facade instead of being silently reset. Existing frozen runtimes continue to own their historical states; deployment/runtime migration is not claimed.

last_verified_candidate contains only source/Run-revalidated results with their original targets and row paths. A damaged history yields no trusted result for that metric. This is an offline candidate report, not formal publication or proof of new filing acquisition. Native source bodies, runtime, Runs, rows and control histories are archived; every archive member is read back against SHA256 and size. CLI reentry uses a copy whose extra control/history records are archived and duplicate data/Run/rows are fully indexed. Python3.9 reentry uses the original installed runtime/history. No noGit lifecycle portability is claimed. Compressed indexes retain exact original bytes and hashes. Independent review, core D03/D04/B13 work and full production acceptance remain pending. All new provider/paid/SEC calls are0/0/0.
