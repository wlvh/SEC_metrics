# D04 特定活动延续与请求开销核对

四段实际保存材料中的招聘、用户及融资渠道延续说明原先被当前检查保留未决。新检查以继续执行的动作及其对象区分特定活动与主体持续经营；含真正疑虑/going-concern文字的句子不走该排除规则。保留原件原句，在测试工厂中检验正确其他含义不再误拦截，错标持续经营疑虑仍拒绝。13项D04测试、108fast和原工厂/原身份接线2项通过；没有真实模型验证或独立代码批准。

当前D04原分组为380请求。对JPM当前输入的离线表示试验：仅扩大样式共享无收益；共享事实重复属性/命名空间并引用重复XML片段，可将111请求降到71，再降到60。每次完整恢复所有130原单元。试验没有修改正式序列化器、当前请求或调用权限，也不代表模型能正确理解新表示。

另一项核对使用官方DeepSeek-V4.1-Flash公开分词文件及纯文本格式参考，固定Hugging Face提交dba1be0a40aa45a94ad051997016db3960a90277。只下载约6.5MB分词/格式文件，没有模型权重、推理请求或账户操作。计入JSON response_format提示后，对原51–53、60–68共12份请求的本地token计数与原API prompt_tokens逐一相等，comparison.json保留原请求SHA和数字。

官方来源：https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/tree/dba1be0a40aa45a94ad051997016db3960a90277 ；https://api-docs.deepseek.com/quick_start/token_usage/ 。这些技术资源不是SEC业务来源。实际API usage继续是实际消耗依据，12例相等不扩大为任意API配置保证。

以180000参考token作离线分组探针，JPM从111组变为29组，所有原单元保留；该探针尚未计入候选导航和重新生成的完整请求身份，不是可执行计划。现有运行入口仍按UTF8字节上界计数，200000上下文、8MiB、120秒、零重试和预算均未改动。后续要做的是限定调用格式的计数实现、完整请求与依赖绑定及对应审阅，不能直接把探针当作放宽门禁的依据。

本轮provider/paid/SEC新增0/0/0，原账33/33/35不变。所有真实接受、完整390、更新和生产退出责任继续，不以这些开发检查宣告完成。
