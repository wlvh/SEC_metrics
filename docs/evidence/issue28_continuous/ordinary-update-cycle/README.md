# Ordinary update-cycle verification

Read validation-summary.json and native-summary.json. The351-file UNFROZEN runtime keeps immutable attempt histories separate from the last verified candidate, checks full source content identities, replays successful native Runs and public rows before reuse, preserves success on failure/withholding, suppresses repeated known withheld inputs, rejects concurrent execution, and recovers interrupted intents or completed terminals before reference writes.

Final material ran eleven actual scenarios in370.207seconds. Two trusted, immutable August/September2026 Marriott submissions bodies differ while the selected2025filing and26186000000USD revenue remain the same. Both form separate candidates; new request identity with unchanged content does not. This establishes recorded content-change handling, not online or new-fiscal-year acquisition. CLI and Python3.9 new-process reentry both reuse verified candidates without a new Run. These reentries retain installed runtime/private history; portable noGit lifecycle is not claimed.

The first and second older-draft runs passed148.245/199.522seconds and are retained as logs, summaries and complete file indexes only. The third failed219.008seconds when the default repository root was incorrectly passed as external source input. Its full original material and log remain. A separate original counterexample accepted an invented latest_attempt reference; its complete material/log remain. The corrected controller validates references, intent/terminal/configuration and the journal chain; the final material rejects that input. Earlier evidence is not re-signed under the final draft.

All final material, including source workspace, actual candidate data/runtime/Run/row files, failed/withheld/interrupted histories and source execution records, is archived. Every archived member was read back against SHA256 and size. CLI duplicate data/Run/rows are indexed rather than copied; its additional control/history records are archived. Exported source checkpoints were compared with actual fixed installed private registrations. compressed-originals.json records exact lossless index compression. MANIFEST.sha256.json binds every packaged file.

The full45-selector source suite passed452.792seconds before a selector-only CI fix; the fast97-selector suite passed78.789seconds. The delivered338bbc8 CI34729030720 had seven successful jobs and a source-material timeout: six B06 test methods shared one240-second cap. Splitting all six into individual selectors preserves assertions and each240-second limit. Those six passed15.891..45.074seconds each; current source count is50, with no claim of a complete local50 rerun. The original failed CI remains archived. New30-minute ordinary-update-cycle CI runs the actual eleven-scenario material.

Independent review is pending. No resident scheduler, live fetch/model execution, runtime permission migration, formal adoption, freeze, publication, merge or active switch is claimed. B13/D03/D04, full390, producer/repair retirement and production confirmation remain required. Real provider/paid/SEC calls remain0/0/0.
