# V13 普通入口的实际 OPEN 验收

本轮绑定未冻结草案 `sha256:fb76e3804bc76494f4efa7fa9d8c188f4b55413d0d1de281b3ebf5ce9348324e`。没有冻结 V13、正式发布或新增 provider/paid/SEC 调用。

14 个实际坐标中，11 个完整 OPEN 图通过：JPM 六个金融指标、Marriott/Lumen B06、Marriott/Paramount C02、JPM D02。金融期间分别保留来源披露的季度平均、年末时点或全年；两个 B06 为 `NOT_MEANINGFUL`，只核验真实非正权益，债务完整性未被断言。

最后三个新安装案例（Marriott A03、Paramount C03、Salesforce C04）被执行文件校验拒绝。原因是执行期间主 agent 更新了绑定范围内的 `tools/run_fast_tests.py`，新拷贝的文件不再匹配本轮草案。该编排错误及三份首失败保留；它们不是业务负例通过，也不是当前后继版本成功。应在最终草案文件稳定后新建目录补验，不能修改这些旧目录来冒充原轮成功。

独立复核另有两个合法实际图和14个有效文件负例。它直接验证旧 `ai_table` 规格的自洽不适用图不能绕过普通入口，以及仅缺来源记录、改期间、缺/多规格等均被拒绝。报告限定于当时精确文件，首轮测试工具的格式/分类错误另行保留，不计验收信用。

C02 的两种实际来源（DEF 14A、同期间 Part III 10-K/A）和 JPM D02 已通过 OPEN 公共行预览：既有20/18列、完整原文引述、来源申报类型/日期均保留。预览不是冻结结果或完整发布包。

最小文本输入另完成11项测试及20坐标在无Git数据根中的独立准备；原消费参数和候选内容逐项未变。必要来源并集为40项（31个原IMMUTABLE_ATTEMPT、9个原LEGACY），不重写类别、失败或旧C04事实。

`archive-index.json` 保存所有归档文件的原位置、哈希和大小。完整原件、运行图和逐文件变形仍保留在索引指向的外部材料目录；生产代码不读取这些测试答案。最终冻结/冷读、完整390坐标、普通新来源更新及旧路径退出仍待完成。
