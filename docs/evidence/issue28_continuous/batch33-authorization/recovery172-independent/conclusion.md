# 172 受限恢复补丁的定向独立审阅

审阅差异：`91f00898c489985ec88836c8408ee07df4617932..2598ce82f0e36f0197f10396913b8ed5ade04c78`。结论：**在本次指定范围内未发现阻断性缺陷；LIVE 恢复仍处于待明确批准状态，不能申领下一次 provider 调用。** 本结论仅覆盖代码、当前账本的只读状态和禁网录制路径，不签发恢复权限、真实模型成功或完整 D04 公司结果。

- **待批准阻断。** `config/issue28_recovery_172_v1.json` 为 `PENDING_EXPLICIT_USER_DECISION`，没有转录链接或记录。`authorization()` 在此状态直接拒绝，`install_live_authorization()` 不写 `recovery-172.json`；若出现该记录则拒绝。当前 LIVE 账本只读重放为 172 槽、累计 `[123,123,49]`、PROVIDER 停止，未安装恢复记录。批次 `claim_fields()` 对 172 后的其他 provider 组仍拒绝新停止，对同摘要第二次申领也因缺恢复授权拒绝；普通未标记 provider 申领同样被拒绝。
- **原件与范围。** 当前第 172 槽的 intent/terminal ID、请求摘要、请求与来源文件 SHA-256、`D04:enphase_energy:0` 组和批次授权 ID 与新配置一致。终态为 `FAILED_TERMINAL`、`HTTP_402`、`[1,1,0]`，没有 `wire/assistant-output.bin`。代码还检查原 172 为批次第 0 次尝试、曾消费 171 的恢复标记；账本逐槽校验前驱和原始证据。110/111/171 原槽仍可只读，补丁未改写旧 110 恢复规则、171 授权或累计总上限。
- **一次申领与新停止。** 新恢复标记仅在目标组、原摘要、单次 172 HTTP402 前驱及已安装有效授权同时成立时产生。追加 claim 后即设置 `recovery172_consumed`，进程重启时从追加账本重建；同一目录锁使并发仅有一次可获准。申领后删除授权使冷读失败，申领前删除则保留 PROVIDER 停止。新 HTTP402/UNKNOWN 等停止继续生效，旧 171/172 记录不会被删除。
- **成功信用。** 来源收集器只在新槽 `SUCCEEDED` 且完整原生响应重放通过后，才从当前失败列表移除旧 172，并登记 `recovered_batch_http402_ordinals`。注册输入冷读会重新核对批次前缀、授权、旧 172 终态及新成功槽；未成功时旧失败仍在列表中。新增字段缺席时维持旧读取路径，V14 快照加载通过。

独立执行了指定的三个 unittest 模块：36 项全部通过。另在内存中移除录制脚本写回既有摘要文件的语句后执行其原有禁网流程；`offline.log` 记录同摘要新成功、先前三槽字节不变、仅 1/6 D04 请求完成、真实调用 `[0,0,0]`。该录制不能证明模型在真实服务上的输出或 LIVE 授权后的完整路径；当用户另作确切恢复决定并完成服务器转录与最终绑定后，仍需针对新授权字节做限定复核及真实终态核验。
