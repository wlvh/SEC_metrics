# Enphase B13 V4 continuation: scoped independent review

verdict: PASS_SCOPED_ENPHASE_V4_ROLLOUT
patch_sha: dcddfa8360dd0fb1a9326ad3fde5fd0f36c536a1
scope: Only the V4 continuation after live Enphase B13 group 1 failure at ordinal 191, its group mapping, claim/replay gates, and submitted offline evidence. This is not a full B13, full PR, production, or future model-output approval.

## Findings

No blocking defect found in this scoped patch. I compared the exact patch with its parent and read the original ledger entries 189–191. Entry 189 is group 0 attempt 0, `FAILED_TERMINAL`; 190 is the same group’s single repaired attempt, `SUCCEEDED`; 191 is group 1 attempt 0, `FAILED_TERMINAL`. Each counts `[1,1,0]`. The original 191 response SHA is `4283913469a91dc3c547e94818fbf72341fbfeb041080fc74ff398bb2ef3716e`, `finish_reason=length`, output 4096 tokens. Independently parsing only complete prefix arrays yields 162 unique `other_context` findings, four source units and zero required candidate rows. This supports the **same excessive nonrequired-output mechanism** as 189 (where 115 of 141 complete rows were `monetary_credit_capacity`); it does not establish that all semantic classifications are correct. See `original-check.log`.

The original user delegation grants one base execution for each of 33 named business groups and at most one new, substantively repaired execution for a failed group, all within 66 provider/paid calls and the original 240/240/80 ceiling. The patch maps group 1’s V4 digest to attempt index 1 only after its exact 191 failed terminal, an evidence-bound repair record, and a matching installed receipt. A second repair claim is rejected. Groups 2–5 have no prior attempts and use V4 at attempt index 0; they do not inherit group 1’s repair slot. Their V4 digests are distinct from each other, all 33 initial digests, and 190. The V3 digests for groups 1–5 are superseded for **new** claims once the V4 record is installed; previous claims remain valid in cold replay. The existing exclusive ledger lock, append-only claim log, per-group attempt state and 66-call batch cap jointly prevent parallel claim consumption. The scoped test covers one repair, one unopened base claim, a forbidden third claim, missing receipt, retained old failure, and cold reread; the existing suite also covers interprocess claim locking.

The V4 preflight preserves all source units, source identity and required-candidate sets in groups 1–5, retains the 4096 output limit, and reports each request within the existing context limit. The V4 change directs the model away from enumerating optional monetary/product/sales/other background while continuing to require review of every supplied unit and every required item; the response decoder rejects those nonrequired excluded rows rather than silently deleting them. On a new 402 or unknown outcome, the common stopped-channel state still prevents another claim; no V4 path clears such a stop. Original 189/190/191 intent, terminal, wire and predecessor identities remain checked in the live ledger and batch history. The synthetic complete response and recorded factory/controller chain establish only that a valid response shape can traverse the new path. They do not give 191 success credit or prove the real model will identify all physical-capacity evidence.

Independent command: `PYTHONPATH=scripts /tmp/sec_metrics_ci_20260922_venv/bin/python -m unittest -q tests.vnext.test_continuous_batch33 tests.vnext.test_continuous_call_ledger tests.vnext.test_capacity_reference_contract` — 44 tests passed; see `targeted-tests.log`. I did not rerun the submitted 127-selector suite, long source material, recorded factory script, or any paid/SEC call.

At this reviewed SHA the new policy is deliberately `PENDING_PATCH_TEST_AND_SCOPED_REVIEW`, with proof fields unset, so this conclusion **does not itself install a live opportunity**. Before any live claim, the executor must bind this exact reviewed patch and proof/log bytes, verify the final execution authority and current no-network factory/controller wiring, and install the one record against unchanged ledger counts and predecessor history. A later source-code change needs its own difference review; changing only the proof binding does not make this conclusion a review of unexamined code.
