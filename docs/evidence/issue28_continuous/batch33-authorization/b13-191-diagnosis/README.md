# B13第191次：Enphase第1组的第二个输出截断

191是已授权批次内Enphase B13第1组的首次V3请求，申领后计入一次provider/paid。原始响应的`finish_reason=length`，输出达到4096令牌，JSON未完成，原生终态`FAILED_TERMINAL`、`DEEPSEEK_RESPONSE_INVALID`、空停止原因；没有候选或完整公司结果。原`calls/0191/`不改，账本累计142/142/49，PROVIDER未停止。

只读脚本`analyze.py`逐项读原始响应与保存请求：4个来源单元、0个模型必评候选，已经完整输出的162个finding互不重复，却全部是非必评`other_context`。它们消耗了整个4096输出额度。这与189的非必评背景枚举导致截断具有共同的输出机制，但不能据此声称模型的其他分类或所有来源含义已经验证。`summary.json`固定原请求、来源、响应和终态哈希。

已审V4的必评优先和非必评背景约束针对这个实测机制。`preflight_enphase_v4.py`从现有完整保存来源构造第1—5组的V4请求，各组全部单元与必评集合和V3一致、4096输出限制不变、上下文均在原上限内。第1组原失败可在实质修复、回归和限定独审绑定后仅补验一次；尚未申领的第2—5组若改用同一修复，各自仍只能申领原本的一次基础机会，不增加批次66次或总240次上限。第0组190真实成功直接复用，不重发。

`v4_group1_shape.py`只用**合成的**完整四单元、零finding响应证明原生接受可处理合法形状；`recorded_factory.py`再通过请求工厂和接受控制录制第1组的一次补验及第2组的一次基础申领，当前两组可冷读、原失败留在批次历史。两者均不是模型答复，不给191成功信用，也不证明财报语义正确。限定回归46项、fast127个selector通过。新策略在补丁、回归和独审绑定前保持`PENDING_PATCH_TEST_AND_SCOPED_REVIEW`；之后只对既定组开放按申领计数的机会。原189、191失败及190成功均保留，D03与生产仍无权限。

**真实第192次已执行且失败。** 它是第1组唯一修后机会，申领标记`batch_attempt_index=1`，计入provider/paid各一次；`finish_reason=length`、输出4096令牌、`FAILED_TERMINAL`，无候选，PROVIDER未全局停止。`analyze_192.py`只读原件：完整前缀156条不重复finding中155条仍为非必评`other_context`，另1条为`physical_capacity_context`；还有一条理由超出128字符。V4未能阻止模型枚举背景，因而不能将录制正例或首组190成功推广为第1组成功。191原失败与192修后失败同时保留，**第1组不再有本批重试机会**。

第2—5组的V4摘要虽已绑定为各自首次机会，但192显示相同方案存在共同输出风险；暂停这些组的真实申领，不把它们作为继续试抽。第2—5组机会未消费。本目录从191审计之后新增真实调用为192的1/1/0；离线审计本身0/0/0。后续要解决的是产生完整且正确的模型输出及必要的权限/方案决定，不能简单放大4096限制、篡改旧响应或把失败重新算成功。
