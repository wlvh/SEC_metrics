# B13第189次：原始响应的有界离线诊断

第189次为Enphase B13 V3第0组、对应历史111的后继业务请求，已正常申领并计入一次provider/paid。服务端`finish_reason=length`，输出恰为4096令牌；响应不是完整JSON，原生接受以`DEEPSEEK_RESPONSE_INVALID`和`FAILED_TERMINAL`结束，未触发PROVIDER全局停止，也没有候选成功信用。原请求、来源、raw response、wire及终态均保持在固定账本`calls/0189/`，本目录只保存可重跑的只读摘要与哈希。

完整前缀中有141条不同finding，其中115条是**非必评引用上的货币/信贷排除项**；其余为4条物理产能语境、5条规划产能、12条产品/装机容量及5条销售/出货背景。重复输出不是这次的第一机制。五个必评引用均已在前缀或程序原生证明中覆盖：B809的“约每季度五百万微型逆变器综合制造产能”由程序证明为可用产能，提示明确无需模型重复，不能把它缺于模型列表误报成漏评。另有五条已完整输出的理由超过协议96字符，其中B454和B1022是必评项。截断本身已足以拒绝，超长理由说明即使仅删除末尾也不能给189成功信用。

可观察事实支持一个有限开发方向：保留全部五个来源单元和必评项、4096输出限制，把可选finding限定为真正与**物理制造产能/实际产量**相关的正向语境；普通货币、销售及产品容量排除项若不是必评候选，无须逐条枚举。现有V3文字已经说“无关非必评块无需finding”，却没有阻止本次大量枚举；模型为什么忽略这句话尚不能从单次响应确定。需要同时处理理由长度约束，不能自动删行、截断理由或给旧189补成功。

B1022有关存货成本吸收和工厂正常利用率。当前限定检查测试明确允许它作为`CAPACITY_QUALITATIVE`，只禁止误当产品装机容量；因此本诊断**不**把这条现有分类直接改判为错误，也不借189改变业务定义。189在该条还返回了`UNRESOLVED`主体；原文是注册人第一人称陈述，因此截断以外也不满足原生接受。任何新协议必须有反例和独立差异审阅，且一次修后补验只能关联189的真实失败和实质修复；其余Enphase组或Ford不借此获得额外抽样机会。此目录新增真实调用0/0/0、完整公司结果0。

V4限定候选仅对这个第0组改变请求表达：必评优先、非必评货币等背景不逐条输出、理由长度128字符，并明确第一人称注册人主体。五个来源单元、必评集合和4096输出上限未变。`v4-counterfactual-summary.json`使用189已完成前缀构造离线反事实：保留其原B1022主体时不通过；**另造**一份把B1022主体改为注册人的正例，才通过来源检查。`v4-recorded-wiring-summary.json`把该合成正例接入录制的请求工厂和接受控制。两项离线正例都不是模型真实答复，不能给189成功信用，也不能证明下一次模型会分类正确。V4补验记录在限定补丁、回归和独审未绑定前保持关闭。
