# C04 保存来源批次路线限定独立审阅

**结论：PASS_WITH_BOUNDS。** 审阅对象为 `c067ffd95d3e51a6e13a7c1fed864725423c567f` 相对其父提交 `8bec108f8029e6b535a266fd0dbc055d09b060ab` 的增量，仅判断 `tools/vnext_normal_candidate.py` 是否在保存来源批次中自动选择现有 C04 四形式后继。没有发现该增量导致错误接受、其他指标改道或证据身份漂移。结论不授予生产采纳、正常新来源获取或全十公司批次信用。

- **其他指标：** 差异只在请求包含 C04 时写入 `request.json.c04_event_forms`，并且仅在 `metric == 'C04'` 时向 `install_normal_inputs()` 和 `create_normal_run()` 传四形式参数；其他坐标仍传空 `route`，函数调用的实参语义与父提交相同。未重跑十公司多指标批次，因此这里是精确差异和接口核对，不是全批次实测结论。
- **不能因缺同 CIK 前期事实给 0：** `normal_run_v3.prepare_case()` 收到四形式参数才进入 `prepare_c04_registration_case()`。后者的 0 分支要求旧 `resolve_c04()` 给出 `names_differ is False`；旧解析器仅在当期、同 CIK 前期审计师事实都为 `FOUND` 时赋予布尔比较。现存 Paramount 批次选择记录为 `NO_SAME_CIK_PRIOR_IN_COMPLETE_SAVED_SUBMISSIONS`，`prior_filing_check=None`、`names_differ=None`、Item 4.01 命中为空；实际原生 Result 是 `WITHHELD/null/C04_COMPARABLE_AUDITOR_FACTS_MISSING`。这是对该已保存输入及代码分支的确认，不等于业务上已经证明本年审计师未变。
- **与先前冷读的身份：** 直接只读比较 `/private/tmp/issue28-c04-batch-20260926-01` 和先前独立冷读使用的 `/private/tmp/issue28-c04-four-form-20260926-03`：两份 manifest 的 Run ID 均为 `run:ordinary-integrated:0c4eb986dc08a25b227b1bfd7ae8727bb79ee1bcd4d12cd08d8417a74cebfdf0`；两份原生 records 的 C04 Result ID 均为 `sha256:ccb7957bbbf6de55aaf2c03aa566ba64344810cc263eb5374315ff5e100c9cbc`，值、原因及公开状态也相同。两份公开格式文件逐字节相同：`metrics_matrix.csv` SHA-256 为 `9ffac1d2c445e3f5d9cbc3d04655f961dd25fb457c423fa6f6c140c6da7eec00`，`metric_evidence.csv` 为 `d5890c5affa63b84603efcee20665991218b44b094e60f3abba99eb96e0341c8`。这也与既有 `summary-shard-repair.json` 和 `cold-shard-repair.log` 相符；本次没有重跑长安装冷读。
- **失败与退出状态：** 首次 `batch-route.log` 在 `verify_batch_route.py` 第 18 行导入 `tools` 时即以 `ModuleNotFoundError` 结束，发生在调用批次主函数前；`batch-route-retry.log` 则记录坐标为 `WITHHELD_CANDIDATE` 并通过脚本断言。批次主函数返回 **2**，因为 withheld 坐标列入既有 `gaps` 判定，`summary.json` 因而为 `COMPLETED_WITH_GAPS`。重试运行的是直接调用 `main()` 的验证脚本，其**进程**成功退出 0；正式 CLI 的 `sys.exit(main())` 才会把这个返回值转成进程退出码 2。原证据里的 `batch_exit_code: 2` 是真实的主函数返回值，不能误读为重试脚本的 shell 退出码。

核验执行：`python3 -m py_compile tools/vnext_normal_candidate.py` 通过；补丁 `git diff --check` 通过；只读检查了所列日志、两个隔离根的 manifest、Result 记录及公开行字节。未执行真实 provider/SEC 请求、长安装测试或 #47/PR52 工作。现有工作树中 `execution-state.json` 已有其他并行修改，本审阅未触碰。
