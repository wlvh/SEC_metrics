# Integrated V14 OPEN development evidence

V14 / issue_28_v13 is an UNFROZEN draft. Current closure `sha256:3d2cc25bea233e0a99854e1355f562a16c8bee7c19422917fe772a2533512849` has318 execution files and34 enabled routes; all39 Issue goals and the5 remaining lodging/text/B13 routes remain required. No provider/paid/SEC calls, Run freezing, active switch, merge or adoption occurred.

## What actually ran

- New native source fiscal labels preserve Salesforce's explicit2026 and raw DEI/CF2025 conflict, and Macy's2025. Quotation, foreign-subject and Unicode first probes are preserved. Root repairs pass the three ORIGINAL variants and ten original filings;10 fiscal tests pass. The bounded process-local interpretation cache is preceded by fresh source selection and byte/proof verification on every call, returns fresh JSON values and rejects inspector changes.29 source/input/native tests pass131.629s; the earlier uncached28 tests took205.126s. No cache entry grants acquisition or call credit.
-20 generated Spec files recompile to exactly the same existing catalog semantics; B01/B03 originals remain unchanged. The Run input fixes exact dependency/spec/result sets before creation.
- Actual initial OPEN cases include Marriott B03, Salesforce B12/C03 and JPM A03. JPM's actual period is2025-10-01..12-31; Salesforce C03 remains49,379,252 USD for2025-02-01..2026-01-31, now grouped as fiscal2026.
- Final `NORMAL_V14_MATERIAL_ROOT=/tmp/sec_metrics_issue28_continuous/v14-material-role-final PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=scripts python3 -m unittest -v tests.vnext.test_normal_run_v3_material`:15 scenarios PASS111.325s. Five actual baselines: B03 dependency graph, B12, D01 review, Macy's B08 instant/fiscal label and E03 event claims. Ten negative/boundary scenarios reject. Native source roots and original protected report/ledger bytes are checked. No freeze call is made.
- Actual CLI batch `/tmp/sec_metrics_issue28_continuous/v14-cli-marriott-final` produces six OPEN candidates and rows (B01/B03/B02/B05/E03/D01). `/tmp/.../v14-cli-salesforce-final` produces two (B12/C03). Both return OPEN_CANDIDATES_READY. The exact commands are in the execution record below and JSON request/summary; this is not340/390 acceptance.
- Existing20/18 CSV schemas are retained. B03 emits one main row plus4 source observations, B05 exposes the two cash-flow/capex facts, E03 exposes3 matched Item5.02 claims, D01 retains34 exact titles. Actual row replay/byte checks pass for Marriott and Salesforce; an event-only batch separately passes.
- Current local95 fast selectors PASS70.501s.34 source-material selectors PASS211.637s. The corrected shared record/compiler/review checks pass29 tests1.812s; the first wrong test-module names are retained as harness errors. Source/input Python3.9 tests pass20 tests52.640s. Semantic and provider-egress checks pass with explicit external output files.
- Existing V13 frozen Marriott D01 and Paramount C03 still cold-read through the new shared registry/Run code (45/36 records), using their original data-root execution bytes and unchanged immutable rules.

## First failures and actual repairs

The first installer probe applied a source-answer guard to legitimate writes of historical governance receipts; all5 setup failures are retained and the guard was scoped to actual source reconstruction. Then D01 revealed a real write-order failure: results were appended before the review decision. The new creator writes review before terminal records.

Deleting B01's result/trace from a B03 Run was wrongly accepted by the old generic complete-result check. V14 now compares the entire source-rebuilt numeric/claim computation graph, including dependency output. A self-consistent false30B revenue/B03 graph is rebuilt using the real Calculator and rejected against original sources. Initial exception classification also omitted RunStoreError; these were recorded harness failures, not passing negative tests.

Macy's legitimate fiscal2025/instant2026-01-31 exposed a second record-coordinate whitelist needing the explicit V14 generation. Both creation and record validation now recognize the new generation.

E03 revealed missing deterministic Claim records and then a source-role mismatch. Claims are now persisted and checked as part of the exact graph. The inventory SourceReference role and the event collection role are represented separately; the frozen historical event projector is unchanged. Event counting semantics were not changed.

## Evidence limits

Old positive OPENs belong to their actual earlier draft closures. They are not re-signed when a successor draft changes. Original full data roots remain under the explicit external material paths; only concise logs/indexes, two complete outcomes and public-row files are copied here. No new V14 FROZEN record exists.

GitHub run34673608483 on the earlier pushedffeaed3 passed95fast/314 tests and32source-material/128 tests. That remote success is included as history, not CI for these uncommitted V14 changes. After the next commit/push, inspect actual CI separately. Full Stage12,390-coordinate acceptance, new source refresh, all remaining semantics, production publication/rollback/retirement and final confirmation remain unfinished. ExistingR3 active/previous are unchanged.

## Actual CLI commands

```
PYTHONDONTWRITEBYTECODE=1 python3 tools/vnext_normal_candidate.py --output-root /tmp/sec_metrics_issue28_continuous/v14-cli-marriott-final --company marriott_international --metric B01 --metric B03 --metric B02 --metric B05 --metric E03 --metric D01
PYTHONDONTWRITEBYTECODE=1 python3 tools/vnext_normal_candidate.py --output-root /tmp/sec_metrics_issue28_continuous/v14-cli-salesforce-final --company salesforce --metric B12 --metric C03
ORDINARY_PROJECTION_MATERIAL_ROOT=/tmp/sec_metrics_issue28_continuous/v14-cli-marriott-final PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=scripts python3 -m unittest tests.vnext.test_ordinary_projection_material
```
