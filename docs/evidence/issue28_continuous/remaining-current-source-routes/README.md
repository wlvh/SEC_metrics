# Remaining ordinary routes with current source admission

Read validation-summary.json. Four current wrappers compose frozen source-selection, semantic, period, Spec and Calculator functions with installed source admission; the frozen files are unchanged. Nine original-source case dictionaries match exactly. The final350-file UNFROZEN runtime creates twelve actual OPEN Runs across Marriott governance/text/B06 guard and all six JPM financial metrics, plus three positive B06 Runs for Enphase/Macy/Paramount. The data bodies remain the same saved SEC content and every source admission is RECORDED_TEST_ONLY.

Default/Python3.9 cold replay covers D01 review, the B06 guard, A13, A03 and positive Paramount debt using the copied runtime without Git/network. The actual positive/negative verification replays all twelve cases, compares old numeric meanings, requires SYSTEM text review and rejects a re-signed false LCR graph and removed D01 approval. The first false-graph test stopped because it supplied extra Calculator target fields; its original failed log/control is retained. The corrected test uses the exact five fields and passed314.233seconds in a separate attack directory, without changing business assertions or the twelve successful Runs.

Archives contain complete candidate data/runtime/Run/row/execution records and changed controls. Duplicate source workspaces are recorded in full file indexes instead of copied again. Every archived member was read back against its SHA256 and size. Large original log/index JSON bytes are losslessly compressed; compressed-originals.json records their original hashes and decompression checks. Exact installed private registration records were compared to exported checkpoints before packaging. The ordinary baseline regression has fifteen passing scenarios and unchanged protected state.

CI splits the twelve-route material by company into45-minute jobs, preserving assertions; measured local creation and verification were845.782+314.233seconds. Previousb1dea1a CI completed allsixSUCCESS, not credit for a later head. Independent review is pending. No fresh financial contents, live SEC/provider call, old quota reuse, freeze, adoption, merge or active switch is claimed. B13/D03/D04, actual new-filing acquisition/trigger/lifecycle, full390 and retirement/publication/final production confirmation remain required.
