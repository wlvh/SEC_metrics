# Update history record identities

The original actual counterexample changed a saved terminal attempt_id and then a terminal record_type, recomputed each record hash, and obtained NO_SOURCE_CONTENT_CHANGE without a new Run. No financial value was changed or falsely accepted; the defect was inconsistent update history accepted as valid. Original records, copied data/runtime/Run/rows, helper and outcomes remain intact.

The correction binds every intent/terminal type and attempt id to its file directory, validates supported terminal states and corresponding intent/configuration, and checks the successful predecessor over the complete linear history. A missing terminal before an existing successor rejects; an unfinished final intent retains the existing interruption-recovery behavior. Hash recomputation cannot substitute for these relationships.

The existing real source-change/repetition/withholding/failure/recovery/concurrency/CSV checks run under the new UNFROZEN351-file runtime. Ten additional actual altered-file cases cover newest and older terminal identities/types, unknown state, changed intent/configuration, changed older intent type/success predecessor and removed older terminal. Intent mutations also re-sign the paired terminal binding, testing more than hash mismatch. Altered inputs are retained individually under metadata-rejections. Restored history still reuses the original successful Run. Python3.9 new-process reentry retains installed runtime/history and proves no new Run; no noGit lifecycle claim.

Every archive member and gzip original was read back against SHA256 and size. Installed source registrations were compared to exported checkpoints. This is root self-review and mechanical verification, not independent review or production acceptance. No new business source/model calls, fee authority, freeze, merge, adoption or active switch. Core D03/D04/B13, genuine acquisition/continuous runtime, full390, publication/retirement and independent review remain required.
