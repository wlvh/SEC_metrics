# B13 原生开发增量与来源分组修复

本增量从6e5a85b继续同一Issue总委托、原分支及Draft PR43。没有新增真实调用；先前固定账本18/18/35及全部旧失败保留。仍无生产权限。

## 已验证

- B13完整来源经过共享表示后可逐字段恢复；JSON键顺序变化不改变请求。两公司仍保留原81单元；当前请求分组为Enphase26/Ford42，每个实际body均通过原200000字节上界估计。分组数不代表已执行真实请求。
- 新原生请求接线13.919秒、原D04诊断回归12.415秒通过，网络/DNS/SEC均禁用；旧诊断入口不授原生信用，B13误用会在计账前拒绝。
- 26个明确MOCK响应→创建者登记→既有Review/Calculator/普通OPEN Run及公共行，完整测试158.619秒通过；重签遗漏请求和测试改LIVE拒绝。季度五百万产能只保留原文，没有年化或利用率。完整测试不是模型语义证明。
- 原V14五个真实基线及文件反例106.457秒通过；106fast通过81.175秒；语义静态检查通过。
- 6e5a85b的CI34837885217实际为9成功/1来源测试超时。剖析在318秒主动中断，仅用于定位，不能算PASS。其主要时间在重复JSON校验。修后JPM完整原件/三种响应冲突检查21.593秒通过，原240秒限制未提高；优化前后两公司81单元完全相同。

## 材料与恢复

`native-material-objects.tar.gz`含1132个唯一内容对象，`material-index.json`恢复4924个文件；所有对象均已读回核验。它包括最终完整B13、共用V14回归、当前B13/D04接线及日志。采用内容去重，不依赖当前仓库里可变文件的旧字节。文件不是新增来源获取信用。

```bash
python3 restore_material.py --evidence /path/to/this/folder --repository /path/to/SEC_metrics --output /absolute/new/review
```

复制运行包的Python3.9禁网冷读已经通过；压缩包还原后的独立回读记录另见`restored-python39.log`。`prior-v14/`与`prior-v15/`保存本增量之前的快照；只更新当前未冻结执行绑定，不重写旧Spec、Run或失败。

## 首次失败和限制

B13前两次接线在外发前触发上下文限制：起初分组过大，随后发现每请求误带整份财年核对材料；已沿用现有五字段解释并核对实际body。第三次暴露JSON字段顺序引起的表示不稳定，已修复并加入序列化重读测试。

完整Run首轮因支持性SEC元数据没有filing accession而失败，修后复用现有SUBMISSIONS标识约定。第二轮正向Run和两个拒绝都发生，但测试误期待ValueError而非RunStoreError；整轮仍是FAIL，日志中的提前PASS摘要不作为验收证据。第三轮完整通过。所有原日志保留，失败目录保留在本机临时材料根；此包没有声称包含所有临时目录。

真实完整B13、数值/有依据缺失/其余不适用坐标、正常更新自动调用编排、D04、390、统一发布/恢复及旧入口退出仍未完成。新模块独立审阅待补；D03仍需已请求的模块报告后才能做必要真实验证，9月17日前不重试旧Codex审阅子任务。本增量不是新停点。
