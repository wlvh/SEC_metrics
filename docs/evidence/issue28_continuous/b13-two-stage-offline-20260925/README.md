# B13 完整来源的最小任务比较（离线，零调用）

本目录在 `task/b06-new-source` 的 `50c341c2` 基础上，直接读取原账本保存的 Enphase 第190次、Ford 第171次 `source.json`。没有修改账本、重发旧请求或查询账户。`measure_saved_sources.py` 使用仓库固定的 `tokenizers==0.22.2` 和完整 Chat Completions 渲染测量输入；`measurement.json` 是量化记录，`measurement.log` 是实际执行日志。Ford来源是第171次保存版本，本次没有把它说成新的当前申报获取。

第192次失败组保留4个完整来源单元、0个模型必评候选，输入156,971、输出4,096且截断。前缀155条 `other_context` 标签只是模型输出，不能证明155处原文都无关。单纯改为旧单元逐一执行仍会留下327块单元；192前缀的142条发现落在该单元，仍有输出风险。

比较以不丢失原件和原190成功为先。单阶段方案A的*示意*是把可见正文切为至多128块的请求，原有非正文单元另保留请求；Enphase第0组仍沿原190合同。按保存来源，A需要约12个Enphase后续请求和48个Ford请求，合计60。这个数不是已通过上下文/跨片段/原生合并验证的可执行计划；分片可能还需要额外上下文和请求。它会引入新单元身份及跨片段关系复原。

选定的离线候选B保留旧17组的全部原始来源单元、顺序及身份：Enphase第0组精确复用原190，其余5组和Ford11组，每组至多一次完整范围引用扫描、一次携带完整原文的判断请求，合计最多32个*新*请求。扫描仅返回单元核对和类型化引用；判断阶段使用原V4类别及来源语义验证，不接收扫描外发现为成功。扫描候选至多64个；超量、截断、引用不实、遗漏必评或判断未覆盖候选时保留未决并停止该组，不裁剪原范围来取得通过。输出上限仍是4096。若某组需要新增来源/上下文或第二个判断请求，也在此方案上限之外停止。

`measurement.json`对B的每一组测量了完整扫描和**合成至多64引用**的判断阶段，实际数量是 `min(64, 该组可用引用数)`；5组不足64，分别为Enphase第4/5组与Ford第8/9/10组。所有输入连同4096输出预留均低于当前200,000上下文上限；最紧的Ford第9组实际使用23个合成引用，判断输入195,083，连预留为199,179。这个压力输入不是模型找出的真实候选，也没有证明真实输出能容纳、扫描不会漏报、类别正确或整家公司能完成。第二阶段仍收到完整原来源和上下文，因此没有把第一阶段摘要当作原文；第一阶段遗漏仍可能导致假阴性，需要获准真实验证与必要人工开发验收检查。两项P2回修后重新运行得到的`measurement-after-p2.json`与原测量逐字段相同，原测量文件保留。

`scripts/vnext/capacity_two_stage.py`在上述初版是**默认不通真实调用入口**的离线构造与验证：扫描检查严格整数的完整单元序号、引用归属、必评引用和64个动态上限；判断阶段必须重新从扫描原始响应字节验证，随后检查身份、原文保留、候选覆盖和原V4语义。空扫描明确标记 `absence_established=false`，当时还没有扫描与判断各自的原生执行保存链。`tests.vnext.test_capacity_two_stage`包含非空正例、错种类/越界引用、漏单元、漏必评、超量、未覆盖候选、布尔/小数序号以及重算摘要篡改反例。回修后定向连同原引用合同共21项通过（`targeted-after-p2.log`）；`tools/run_fast_tests_v2.py --jobs 8` 的128个selector通过（`fast-after-p2.json`，69.059秒）。初版`a48d282e`的限定独审在`independent-review/`保留两项P2未通过结论；`c44bb047`的限定**差异**复核在`independent-review-followup/`确认这两项已修复，不扩成全模块、真实调用或公司结果批准。下文记录随后新增的原生录制链，不倒填初版结论。

原190历史身份在初版按 `ordinary-v14-binding-repair-20260925/no-call-reuse-190.json` 的当时需求闭包结果保留。初版尚未证明混合旧成功与新两阶段的整公司合并，不能凭“意义相似”先算复用成功；后继的精确重放与录制合并见下。原191/192组没有第三次本批机会，32个新请求也不是既有33组授权自动覆盖；必须先给出包含方案、组映射、失败关联和总上限的具体权限决定。当前总账仍应以实时账本为准，本目录零真实provider、零真实paid、零SEC。

## 后继原生录制链与当前边界

显式V5把原必评集合与扫描候选集合分开。被扫描选中但不在原必评集合内的B2382发货背景，可由第二阶段引用原文排除；未扫描的任意背景仍被拒。扫描选中明确的“manufacturing/production capacity”而第二阶段将其列作普通背景时，有限来源关系护栏会拒绝。旧V4合同和历史失败不改；这项护栏是必要关系检查，不保证模型对所有自然语言判断正确。

`recorded_native_chain.py`在禁网录制账本中完成六组Enphase来源覆盖：第0组用原190响应作**模拟对照**，第1组单独保存扫描与判断的请求、原响应、执行证明和前驱关系，第2—5组保留V4合同；七条模拟申领进入原生Candidate/Evidence、登记、普通Run和公开行，安装副本独立进程冷读字节一致（`native-chain.log`、`native-chain-summary.json`、`native-chain-cold.log`）。随后`replay_final_native_chain.py`不再申领模拟调用，直接在最终V15绑定下重验这七条保存执行并重建Run/公开行；独立冷读再次逐字节一致（`final-native-replay.log`、`final-native-replay-summary.json`、`final-native-cold.log`）。两个Run均为`RECORDED_TEST_ONLY`、真实调用`[0,0,0]`，不构成Enphase真实公司结果。扫描阶段的Candidate/Evidence仅确认完整来源引用形状，明确`native_credit=false`；只有保存的扫描和判断同时重验，判断请求才有单组原生接受信用。

原账本190另以当前请求/来源精确字节做**只读**重放，通过当前接受检查，未改其身份或终态（`read_only_190.py`、`read-only-190-final.log`）。这证明原第0组可按旧V4合同保留；录制整公司链证明混合版本的程序覆盖与冷读。它们不能替代后续五组的真实模型输出。按最终完整扫描字段、阶段证明、至多64候选及“全部候选仍未决”的压力输入，`measurement-max-unresolved.json`核对剩余16组的32个阶段请求都处于原200,000上下文边界内；最紧Ford第9组判断为199,648，余352令牌。`measurement-current-native.json`是未把所有候选列为未决的较轻情形，未覆盖此最坏字段组合。上述均是固定保存来源的结构测量，真实相关项漏报、错误排除、输出4096截断及整家公司完成率仍须获准真实验证。原191/192没有第三次旧方案机会，新两阶段真实入口仍明确关闭；32次是新方案上限，未获本批自动授权。

在`7cde5ae0`提交时，定向来源/引用/分组29项与原账本计数控制8项有本目录日志；B13旧数值/文字组件5项由父会话实际执行通过，终端输出未另存本目录。固定依赖的本地128项快速套件以8个并发执行时有126项通过、两项30秒超时；两项在隔离运行中分别20.9秒与17.7秒通过。首轮系统Python缺`tokenizers`的失败与第二轮并发超时均留在`fast-current*`，不能写成完整本地fast通过；远端新head CI仍须另看。旧两项P2与初版限定审阅按原范围复用，不把父会话测试冒充独审。

`7cde5ae0`的新增差异限定独审见`independent-review-native/conclusion.md`，结论**未通过**：同一句“our contract manufacturers have sufficient production capacity”若被标为`OTHER_ENTITY`主体或历史期间，能够绕过当时只检查`TARGET_REGISTRANT / CURRENT_REPORT`背景的护栏。原审阅、反例日志和该提交的完整录制链保留原义。后继有限修补在**显式V5**中针对原文可直接绑定到本公司当前产能安排的断言同时核对类别、主体与期间；真正其他主体的供应商产能仍可排除，明确旧期间不被新护栏升级为当前，原V4不改。修后定向30项通过（`repair-targeted.log`）。从原七条模拟执行**零新增模拟申领**重建当前闭包`sha256:acd297a07773ccd3d181c74d46a3a76f53aa23f3b33726e442f9503d52173637`的Run/公开行并经安装副本独立禁网冷读通过（`repair-native-replay.log`、`repair-native-replay-summary.json`、`repair-native-cold.log`）；公开行与前一录制版本字节一致。原190也在本次闭包下再次只读重放（`read-only-190-repair.log`）。新修补的确切SHA限定独审与新head CI仍待完成；不能把旧独审未通过改写为通过，也不能把录制结果写成真实B13公司完成。

`99d868f5`的限定差异复核同样**未通过**（`independent-review-native-repair/conclusion.md`）：整句扫描“prior years/previously”会遮蔽同句后半的当前产能断言，错误主体／历史标签因而仍可接受。保留这一失败，不改判长链。下一有限修补去掉整句历史词一票否决，改为在产能断言的局部从句处理条件与过去式“used to have”，并保留明确年份和对照提示的有限检查；加入旧期间对照、过去曾有、真实其他主体及混合句完整响应级对照。定向31项通过（`local-repair-targeted.log`）；受影响的原保存V5判断在当前闭包`sha256:8a87eb53dc613a0a542265444a59e70ee6b1b01eac2a9145a49af01c4eb0b8c7`下只读重放通过（`read_only_stage3.py`、`stage3-local-repair.log`），新增模拟和真实调用均为0。扫描、保存、公司Run与冷读代码字节未变，上段完整录制链的原始证据按当时闭包保留；本段**不宣称已在新闭包下重做整条冷读**。当前修补仍待新SHA限定复核和新head CI，不能凭短测或原录制成功推断所有来源分类正确。

`79fe67b1`限定复核再次**未通过**（`independent-review-local/conclusion.md`）：同一可见块先写本公司合同制造商过去产出、后写供应商自己的产能，旧辅助判断跨分句借用本公司主体，误拒正确的`OTHER_ENTITY`。随后只在显式V5中把目标主体与物理产能放进同一分句；仅当紧邻前一分句明确是本公司合同制造商、后一分句以`they have`承接时，才保留跨分句当前断言。新增混合供应商正例与之前的历史对照、旧期间对照，31项定向通过（`clause-repair-targeted.log`）；受影响V5判断原件在闭包`sha256:5a28b5906fcb312ec69a9fbdec4e9f2118e999ddf0c437ea70cc05a2c4878ac6`下只读重放通过（`stage3-clause-repair.log`），新增模拟/真实调用0。旧完整录制冷读按其精确闭包保留，本次仍**没有**重做当前闭包的整公司冷读，也没有真实模型准确性证明。新SHA限定复核待完成；若作用域规则继续产生明确反例，应停止这条词法补丁路线，重新界定扫描引用的断言粒度。
