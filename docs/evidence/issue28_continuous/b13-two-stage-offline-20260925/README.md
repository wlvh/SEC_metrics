# B13 完整来源的最小任务比较（离线，零调用）

本目录在 `task/b06-new-source` 的 `50c341c2` 基础上，直接读取原账本保存的 Enphase 第190次、Ford 第171次 `source.json`。没有修改账本、重发旧请求或查询账户。`measure_saved_sources.py` 使用仓库固定的 `tokenizers==0.22.2` 和完整 Chat Completions 渲染测量输入；`measurement.json` 是量化记录，`measurement.log` 是实际执行日志。Ford来源是第171次保存版本，本次没有把它说成新的当前申报获取。

第192次失败组保留4个完整来源单元、0个模型必评候选，输入156,971、输出4,096且截断。前缀155条 `other_context` 标签只是模型输出，不能证明155处原文都无关。单纯改为旧单元逐一执行仍会留下327块单元；192前缀的142条发现落在该单元，仍有输出风险。

比较以不丢失原件和原190成功为先。单阶段方案A的*示意*是把可见正文切为至多128块的请求，原有非正文单元另保留请求；Enphase第0组仍沿原190合同。按保存来源，A需要约12个Enphase后续请求和48个Ford请求，合计60。这个数不是已通过上下文/跨片段/原生合并验证的可执行计划；分片可能还需要额外上下文和请求。它会引入新单元身份及跨片段关系复原。

选定的离线候选B保留旧17组的全部原始来源单元、顺序及身份：Enphase第0组精确复用原190，其余5组和Ford11组，每组至多一次完整范围引用扫描、一次携带完整原文的判断请求，合计最多32个*新*请求。扫描仅返回单元核对和类型化引用；判断阶段使用原V4类别及来源语义验证，不接收扫描外发现为成功。扫描候选至多64个；超量、截断、引用不实、遗漏必评或判断未覆盖候选时保留未决并停止该组，不裁剪原范围来取得通过。输出上限仍是4096。若某组需要新增来源/上下文或第二个判断请求，也在此方案上限之外停止。

`measurement.json`对B的每一组测量了完整扫描和**合成至多64引用**的判断阶段，实际数量是 `min(64, 该组可用引用数)`；5组不足64，分别为Enphase第4/5组与Ford第8/9/10组。所有输入连同4096输出预留均低于当前200,000上下文上限；最紧的Ford第9组实际使用23个合成引用，判断输入195,083，连预留为199,179。这个压力输入不是模型找出的真实候选，也没有证明真实输出能容纳、扫描不会漏报、类别正确或整家公司能完成。第二阶段仍收到完整原来源和上下文，因此没有把第一阶段摘要当作原文；第一阶段遗漏仍可能导致假阴性，需要获准真实验证与必要人工开发验收检查。两项P2回修后重新运行得到的`measurement-after-p2.json`与原测量逐字段相同，原测量文件保留。

`scripts/vnext/capacity_two_stage.py`目前是**默认不通真实调用入口**的最小离线构造与验证：扫描检查严格整数的完整单元序号、引用归属、必评引用和64个动态上限；判断阶段必须重新从扫描原始响应字节验证，随后检查身份、原文保留、候选覆盖和原V4语义。空扫描明确标记 `absence_established=false`，录制/合成通过没有Candidate、Evidence或Run信用。`tests.vnext.test_capacity_two_stage`包含非空正例、错种类/越界引用、漏单元、漏必评、超量、未覆盖候选、布尔/小数序号以及重算摘要篡改反例。回修后定向连同原引用合同共21项通过（`targeted-after-p2.log`）；`tools/run_fast_tests_v2.py --jobs 8` 的128个selector通过（`fast-after-p2.json`，69.059秒）。初版`a48d282e`的限定独审在`independent-review/`保留两项P2未通过结论；回修需单独限定复核。完整原生双阶段保存/冷读接线仍未建立，因而尚未达到B13候选准备关口。

原190的历史身份及当前精确重放仍按 `ordinary-v14-binding-repair-20260925/no-call-reuse-190.json` 的当前需求闭包结果保留。本候选没有改原190请求；但混合旧成功与新双阶段的完整公司合并尚未通过，不能凭“意义相似”先算复用成功。原191/192组没有第三次本批机会，32个新请求也不是既有33组授权自动覆盖；必须先给出包含方案、组映射、失败关联和总上限的具体权限决定。当前总账仍应以实时账本为准，本目录零provider、零paid、零SEC。
