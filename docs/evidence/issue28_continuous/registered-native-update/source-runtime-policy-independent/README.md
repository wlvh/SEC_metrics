# 来源与执行规则目录：独立有限检查

根执行者审阅独立补丁93f3e606…，对D04、B13和财年入口分别实际调用政策路径/字节核对：默认旧入口仍要求安装目录规则；显式普通入口从当前ROOT读取规则且原件参数仍指向原source_root；当前ROOT规则损坏时拒绝。9项通过。下游原件解析在此探针中故意停止，不将它写成完整来源工厂、调用或Run通过。

补丁尚未应用时运行；完整固定source-inputs工厂检查由作者另存，主线运行绑定/原生更新须在应用后验证。探针路径是本机overlay，交付重跑时需先恢复该精确补丁版本。没有模型或SEC调用。
