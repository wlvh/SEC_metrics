# Issue #28 普通运行 V14 执行绑定与父子身份修复

起点为已推送`2c2aa8de5d070c598141237f45de82ee92b998a3`、干净工作树及原账本143/143/49、无通道停止。主CI`36016700987`总体失败。`107690968984`、两家剩余来源Run以及普通原生Run的正向准备均指向同一个直接错误：普通入口`issue_28_v13`（执行代号`PROFILE_DRIVEN_V14`）的执行清单仍将`continuous_call_ledger.py`绑定为16070字节、SHA前缀`735accb3`，当前实际文件为21018字节、`50ff87a4`。多个反例随后因没有正向Run而报`KeyError`，不能单列成业务语义失败。更新历史与独立指标作业仅在日志中表现为状态断言失败，须以修后原测试另验。

仅更新未冻结的普通`issue_28_v13/baseline_manifest.json`当前执行字节；普通入口仍选择v13，`normal_run_v3._binding()`及共享默认接口不改。后继`issue_28_v14`的父级完整身份和五文件摘要随当前v13更新。`issue_28_v14/transfer_manifest.json`的父级值并非有意代表另一历史版本：在`8346c326`它与当时baseline同为`047e...`，`7804842e`只把baseline父级推进至`1711...`，留下转移声明不同。现在将其与实际父级同步，并让v15加载器明确核对转移声明。旧提交、旧账本、旧Run包和旧失败均未修改；固定审核快照仍由`git show 2c2aa8de`验证，只证明修前材料对应。

本地使用`/Users/lyuhongwang/Developer/SEC_metrics`代码根和全新外部数据根。测试发生在**未提交工作树**，精确文件SHA、根路径和差异见`after-uncommitted.json`，不能称作已提交SHA的测试：

- v13/v14需求加载与360/453个执行文件当前绑定检查通过；8项定向授权测试通过，包括转移父级旧值的拒绝。
- Enphase B06普通OPEN候选真实读取保存来源，得到原口径精确值`1.107959077222837051285943352`，无新请求或生产采纳。
- 普通Run材料原测试5个正向、10个磁盘/身份反例均通过；更新历史原测试359.613秒通过，独立指标隔离原测试367.714秒通过。成功指标不因相邻B06未决而失去候选。
- 当前B13禁网工厂/批次22项通过；第190次原成功在新后继完整身份下只读选择与重放成功，账本143/143/49未变，不代表完整B13公司。SEC原生录制、失败隔离、零重试和独立冷读原测试90.211秒通过；更新后的provider及SEC接线收据均通过当前执行权限哈希核验。
- fast套件127个selector全通过。新provider/paid/SEC调用均为0。

旧主CI的保存来源材料作业`107690969253`从开始至取消约35分钟，符合该作业自身`timeout-minutes:35`；其测试步骤只运行约24分钟，**未得到材料测试终态**。这与上述普通绑定失败分开；没有因本修复机械重跑该大套件，也不把取消写成通过。正式CI、限定独审和必要历史包只读兼容仍按新提交分别确认。无Ready、合并、采纳、部署或active切换权限。

## 限定独审发现的接线P2与修后范围

首个补丁`5c307e55932a8c65dbd05ba8ce44d93b66b7c980`的限定独审**未通过**：普通父子需求修复本身没有阻断，但B13离线接线收据仍声明旧V15完整身份`c26a...`，而修后实际为`1f11...`；`validate_wiring_receipt()`当时只检查执行文件集合哈希，漏掉这个已经存在的完整身份字段。原结论保留在`independent-review/conclusion.md`，不改判通过。

修后增量只让校验器同时要求收据的`requirement_closure_hash`等于实际加载需求身份，并将当前收据同步到新的V15完整身份及执行文件集合；SEC收据也随实际执行文件集合同步。新增负例把旧完整身份注入当前收据，现可被拒绝；31项定向禁网测试通过。更新后的V15完整身份为`b482d46b2ba4a4e34e394974df5b62d3f8a5419fced4aae17630a9172840de8e`，v13普通身份仍为`ecaef229...`。第190次原成功在该最终身份下再次只读选用成功，原账本143/143/49未变。原359秒、367秒及15项Run材料的执行输入中，普通v13绑定与共享Run代码未再改变；不为这个收据差异重复整套长测试。新增差异仍须按确切新提交限定独审与远端CI验收。
