# Bond and separate-lease development evidence

Read validation-summary.json first. The final338-file runtime has three actual OPEN B06 Runs/public previews and four rejected counterfeit/old-Spec graphs. native-batch.tar.gz and native-attacks.tar.gz were read back against every member SHA and size. Native cold replay uses the copied runtime without Git and with socket/DNS blocks.

The earlier336-file batch passed warm native replay but failed cold import because an existing financing module and its policy were absent from the runtime package. Its exact authority, Run records and whole-tree index are retained; the original full directory remains at the path in first-336-full-file-index.json. It is not relabelled as the338-file success.

Source failures and harness mistakes remain explicit. The first date-mutation harness dumped large original XML into stderr; it is losslessly compressed, not omitted. The comparative test now requests only the borrowing reconciliation operands; no previous finance-lease-total fact or previous primary acquisition is invented. Coupon/source-value discrepancies are retained in source evidence and unused in B06.

No external business call, independent-review approval, freeze, adoption, deployment or active switch was performed. The full390/update/publication/retirement goals remain unfinished.
