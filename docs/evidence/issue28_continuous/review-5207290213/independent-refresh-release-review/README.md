# 普通有限刷新与统一版本准备：限定独立审阅

审阅者未实施此次增量。范围为`ordinary_refresh_cycle.py`、其显式CLI、`ordinary_release_preparation.py`和对应测试/材料；确切运行文件、测试与材料SHA见`scope-files.json`。旧获取、账本、Run和Projector只追踪必要调用边界，不重开旧模块审阅，不覆盖6e5 D03受限任务。

## 限定结论

在本次差异检查、两个独立协调器探针及归档比对范围内，未发现阻断问题。这里没有重跑506/512秒完整材料；原工程执行者的长材料结果与本审阅新增证据分别登记。发布准备模块及其测试的当前字节与已完成长材料的实施身份一致。

**LIVE就绪边界仍需保留：**审阅时新刷新入口要求的`ordinary-refresh-cycle/wiring.json`不存在，只有明确标为`DRAFT_NOT_AT_LIVE_ENTRY_PATH`的`wiring-draft.json`。因此当前材料不能宣称新刷新入口已经真实执行或完成LIVE接线；在正确安装当前离线证据之前，该门禁会阻止SEC获取。这是待完成的接线登记，不是绕过门禁的缺陷。

## 有限刷新与历史保留

- 单次上限必须为0–80的整数，真实执行仍使用原固定240/240/80总账。协调器不创建新额度、货币预检/预留或账户操作。
- 获取必须使用原生会话，来源根固定为该账本的`source-inputs`；LIVE先核对自身执行字节、原权限/SEC门禁和专属离线证据。实际HTTP路径继续强制零重试。
- 已失败URL与本轮已尝试URL不会被本轮自动再次获取；元数据失败不会因旧来源可读而变成刷新完成。
- capture异常停止继续获取，所有选定公司仍分别进入安全更新处理；来源刷新状态与原生候选状态分开。未接线的B13/D03/D04普通更新路线明确报告，不伪装成披露不足。
- 自身调用按原生收据求和，共享累计差额单列。异常后的无法归属槽显示UNKNOWN/null，不填写0；没有把其他进程消耗算成本协调器的调用。

本审阅运行5项既有边界短测试，0.002秒通过。新增合成探针显式模拟来源发现、会话/账本与历史处理：capture在计槽后异常时仅调用一次capture，不继续获取，报告SEC数量null及`CALL_ACCOUNTING_UNRESOLVED`；两家公司仍被处理，无关公司可以READY，历史引用保留。后续触发面对同失败URL没有capture调用。`refresh-probe.json`明确这些是协调器边界模拟，真实调用0/0/0。

原完整材料另外证明保存原件上的Pfizer B01数值、JPM A08数值和JPM B01结构性结果、相同内容重复触发、元数据内容变化、远端失败/本地输入故障和恢复。84秒接续材料保留原成功引用和14个测试槽；它们均为记录来源获取，不是新真实SEC获取或新财年验收。

## 统一版本准备与前驱

准备器先读取真实PublicationView前驱，再复制完整前驱及选定原生输入/Run；受信当前代码重新执行原生重放和公共行生成。主结果不可用、来源字节变化或Review图不完整不能成为选定结果。代码只调用既有Projector合并函数，不产生生产许可、publication manifest或active切换。

独立读取已完成归档得到：

| 项目 | 结果 |
|---|---|
| 准备ID与所保存内容 | 相符 |
| 准确前驱manifest SHA | 相符 |
| 327个矩阵行中的325个继承行 | 与前驱逐字段、顺序相同 |
| 319个证据行中的288个继承行 | 与前驱逐字段、顺序相同 |
| 两个选定原始来源文件 | 与source_locations所列SHA相同 |
| 选定类型与来源信用 | Pfizer B01为NUMERIC；Salesforce C02为TEXT_V1；均保留PREEXISTING_SAVED_ACQUISITIONS_ONLY |
| 准备信用 | NONE_PREPARATION_ONLY，无publication_manifest，production/switch/full390均false |

`release-audit.json`和脚本保存本审阅的精确归档SHA与上述比对。原工程长材料已记录重签公共数值被原生组合拒绝、删除真实Review记录被原生审核图拒绝、原输入移走后重验，以及实际指针未变；本报告核对了这些测试和结果，没有把本次CSV比对冒充再次执行全部原生攻击。

## 剩余责任

LIVE刷新接线证据安装、全部39项普通更新、真正新财年、全部390当前验收、正式采纳/发布适配、旧入口退出和集中生产确认继续由总委托承担。327行准备包不等于390完成，388未选定坐标也不等于388开发失败。该审阅不是用户本人审阅、全PR批准或生产权限。

本审阅未修改运行代码、原材料、固定账本或实际指针；新增provider/paid/SEC为0/0/0。
