# B06 note debt development evidence

See `validation-summary.json` first. `final-native-batch.tar.gz` contains both current actual OPEN Runs, original inputs, runtime and public previews; `first-native-attempt.tar.gz` preserves the first period-binding failure. Each archive was fully read back against its adjacent SHA index. Generated Python caches are excluded explicitly. `final-native-attacks.tar.gz` retains the recalculated false-debt and old-Spec substitutions rejected by native replay.

The original source-test failure is `b06-note-source-tests-first.log`: a new short-term borrowing tagged with a specific note dimension was initially accepted. The current test retains that original case. Other first failures and exact test logs are retained separately. Source and draft versions are not relabelled as equivalent.

All work used saved sources. No new business calls, old quota revival, freeze, independent approval, merge, adoption or production switch occurred. This is a component of the continuous Issue-level mandate, not its endpoint.

The intermediate `native-batch`/`native-attacks` archives remain the successful28314833draft before the final guard-order correction. `pre-guard-validation-summary.json` preserves that state; currente0894b70native and cold replay were rerun, including a check that nonpositive equity never enters the new debt grammar. Source code was unchanged, so earlier complete source tests were not repeated.
