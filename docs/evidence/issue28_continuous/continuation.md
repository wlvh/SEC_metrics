# Issue28当前入口：B13取消计划否定误拦截已回修；111历史成功与当前信用分开

原目录、task/b06-new-source、同一Draft PR43，只追加提交。实际HEAD以git/PR为准。本轮没有provider/paid/SEC请求，没有账户操作、探针、恢复或重发；PROVIDER仍因171 HTTP402停止。

## 本轮确切增量

170逐行核对110条输出（64个不同finding、46组完全重复），全部引用与对应原HTML字节范围验证。程序零基映射与请求一致；107行没有建立声称的可见产能/规划/产品容量来源关系。另3行虽属收入/销售背景，混合期间或引用上下文仍有限制，不能算语义已验收。既有数量验证的请求范围子检查不覆盖这些定性错误；没有把去重版本送成成功结果。

新增有限的必要来源关系检查，仅处理三个可见叙述角色，未证明则返回B13_VISIBLE_SOURCE_ROLE_NOT_ESTABLISHED，不读取模型理由作依据、不自动换标签或推断披露缺失。主体、期间、数字、原生事实/补充XML和完整集合验证仍另行承担；这不是完整B13语义证明。

独审ccd7df0确认来源问题，并提出一个P2：无关“没有债务”误伤扩产计划。此前已回修。新发现的P2是后文`have not abandoned the plan`、`have never cancelled the expansion`被误读成实际取消；现只在取消断言局部识别`not/never`，仍拒绝实际取消、直接否定与条件性计划。新增完整响应级正反例，23项定向测试PASS3.004s、126项fast PASS52.628s；测试和禁网接线不等于修后限定独审，状态仍待审。原代理累计68工具/3条消息，不再复用发消息；未新开重复审阅。

## 111的重要更正

原111 B1022讨论制造设施利用率及存货成本分摊，却标为产品储能/装机容量，并在理由中把facility加入了原定义。原定义并不支持该扩大。原SUCCEEDED、请求/响应、原身份和计数保留；当前验收返回B13_SOURCE_ASSESSMENT_UNRESOLVED，不能再宣称可原样复用。当前factory/opener/controller已禁网证明这一拒绝，同时完整无必评项的录制对照通过。有限下游核对见`b13-170-source-audit/p2-111-downstream-boundary.json`：111仅有单请求候选、无原生完整Result；现有390索引中的Enphase B13仍待验证，所检查的正常更新摘要没有B13或111当前信用，Enphase录制更新材料属于D04。此结论不声称重验了所有历史材料。没有自动改标签或发替换请求。

111使用旧文字标签协议已经出现此问题，因此不能把170的原因仅归于数字编码。所有110行（含原错误和重复）仅作格式成本比较：数字2677、完整名称4350、有含义短标签3112token；这不是模型准确性实验。本轮不变更协议/分组/类别含义/4096上限。

## 其他无provider工作与保留成果

Ford187条原生权益相关事实已整理；当前归母权益35952×10^6美元无分部维度，属于合并数，不能直接替代工业分母或擅减FordCredit。这个标签清单不证明整份报表未披露工业权益。

Paramount九处旧档案原文锚点逐字节复核，EY同意函属Skydance LLC、PwC是履历，不是当前注册人的审计师变更。当前C04v2没有以新注册人明确否定陈述替代同CIK前期AuditorName比较的分支；若需后继规则，先明确可接受的首期审计历史及事件覆盖，不自动给0或跨CIK组合。没有新Ratio/C04值。

当前390类别不变，D04八家完整候选保留。113无损链和114交集修复的实现/长链未重做。共享Run/投影/发布及旧pipeline相关文件与26d9d947字节相同；actual active和镜像未变，既有完整发布/6865还原和Ford长演练证据复用。

## 绑定、CI和授权停点

当前V15开发闭包2de397b，已更新未冻结V14对`capacity_quantity_roles.py`的执行字节绑定；新绑定下最短禁网factory/opener/controller35.949s通过，旧收据被拒，新的实际`validate_wiring_receipt()`及执行权限校验通过，不是只改收据哈希。此接线的成功对照是空必评组，非空合法扩产由完整响应级测试覆盖；二者都不解除171停止，也不替代P2修后独审。

a03f9af6主CI35775898391终态SUCCESS（用户更新及本轮一次`gh run view`）；此前26d9d947主CI35756140686也为SUCCESS。旧35分钟取消只能定位到同一汇总步骤中断，不能归因成某个测试失败；没有抬高时限或重跑已通过大材料。a03f9af6的CI没有覆盖新P2，最终新提交CI按推后单次观察记录，不把旧head全绿当成新head全绿。

账本仍171槽、122/122/49，余118/118/31，上限240/240/80。113/114真实同摘要验证、170原样重发、171恢复均未授权，111当前复验阻断也不能通过替换请求绕过。D03不调用、增额暂缓。

此前“基础缺口44”假定111可复用；若按不变分组另行验证111，待验证组为B13 17+D04 16+D03首轮130=163，对余118条件缺口至少45，仍不含后续验证。这是条件计量，不是申请已获批或立即调用决定；任何获批的不新增调用方案须另按实测更新。

四项B06、Paramount C04、完整390、正常更新及旧入口退出责任仍保留。无Ready、合并、采纳、部署、active切换或长期运行权限，不触碰Issue47/PR52。证据统一见b13-170-source-audit/，独审增量追加在原failures-112-114-repair/independent-review/。
