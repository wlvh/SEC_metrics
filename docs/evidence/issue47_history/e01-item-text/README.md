# E01 的 8.01：从项目自身的正文确认，而不是从程序写的说明句

## 实现错误（已修，只在历史路线）

冻结的匹配器把 E01 的关键词拿去和每条 claim 的 `brief` 比。凡是 hdr.sgml 带条目代码的申报——本仓库保存的每一份 8-K 都带——`brief` 就是程序自己写的句子 `8-K item 8.01 parsed from hdr.sgml`，任何关键词都不可能出现在里面。所以 8.01 分支一份都没读过、一份都没放行过。这是明确的实现错误，不是口径问题。路由器被 `issue_28` 各世代按字节冻结、普通路线原样在用，所以修复只在历史路线的后继规则文件里：`scripts/vnext/historical_event_items.py`（第 34 个规则文件），由 `historical_zero_ai_results.py` 在 E01 分支调用。**普通路线（#28）的 E01 仍带这个缺陷**，不从本任务改上游。

## 读的是什么（来源范围）

8.01 的正文 = 主文件里从该条目标题到**下一个条目标题或签名**为止的文字，在冻结的 `_visible_text` 视图里定位，所以任何人从同样字节能重建同一个区间。标题的判定：`Item x.yy` 后跟大写词、且前面不是指向条目的功能词（"into this Item 2.03"、"under Item 1.01"）或开引号——Macy's 的封面把空复选框渲染成字母 "o"，所以"前面任何小写词都算引用"会丢掉 Macy's 的全部条目，列表必须封闭。同一条目的连续标题（"Item 5.02" 后接 "Item 5.02(b)"）算一个条目；被别的条目隔开的两次标题、以及 "Items 7.01 and 8.01" 这种合并标题，**按名拒绝**而不是猜：读不到的条目既不计入、也不悄悄不计入。

**不读的**：条目按引用并入的附件（EX-99 新闻稿等）。它们是单独文档、本仓库一份没存，而且它们算不算"8.01 正文"本身是待决范围问题的一部分；每个条目记录自己的正文是否提到并入附件。

独立核对：`tools/read_e01_eight_o_ones.py` 不导入路线任何模块、按自己的方式找条目。本仓库保存的 **40 份**带 8.01 的 8-K，路线的读法全部唯一定位；其中阅读工具能找到的 36 份，两边的正文（规范化后）**逐份相同**；另外 4 份是 Paramount 前身把标题写成 "Item 8.01 Other Information"，阅读工具的标题模式不认识。

## 含义未定时路线答什么（过渡规则）

关键词出现在 8.01 正文里是否就算"确认"，是待决的含义问题：已批的子串规则说算，指标名字（并购公告）说融资语境里的不算。已批条款要求**关键词确认**，所以正文里没有任何关键词的 8.01，在该条款的任何读法下都不算确认。过渡规则只回答不依赖这个决定的部分：

- 窗口内每个 8.01 的正文都没有关键词 → 答案 = 直接计入的 1.01/2.01 条数；观察记录绑定每个被读的 8.01（区间、正文哈希、读到的结果）。
- 有任何一个 8.01 的正文带关键词 → **按名扣留** `HISTORICAL_EVENT_KEYWORD_CONFIRMATION_MEANING_PENDING`，每处命中连同所在句子记录下来；两种读法都不默认采用。"必须出现具名对手方"不作为含义采用。

它答的是**现行已批定义**下的值。把 E01 改成"经内容确认的并购公告"会同时去掉关键词要求、并对 1.01 也要求内容确认——那是新定义，有自己的结果，见下文与 `../e01-keyword-branch/`。

## 七个有值窗口（批次 `aa0eff7e` → 本修复）

| 位置 | 修复前 | 修复后 | 原因 |
|---|---|---|---|
| Enphase 2025 | 0 | 0 | 窗口无 8.01 |
| Ford 2025 | 2 | 2 | 5 份 8.01 正文均无关键词 |
| Lumen 2025 | 7 | 7 | 8 份 8.01 正文均无关键词 |
| Southwest 2025 | 2 | 2 | 1 份 8.01 正文无关键词 |
| Macy's 2026 | 2 | 扣留 | 要约收购的 "combined aggregate purchase price" |
| Marriott 2025 | 0 | 扣留 | 两份募资用途句里的 "acquisitions" |
| Pfizer 2025 | 0（已登记缺陷） | 扣留 | 完成收购 Metsera（"acquisition"、"Agreement and Plan of Merger"） |

Pfizer 在两种读法下都是 1，但路线不做内容判断就无法知道这一点，所以过渡期同样扣留；修复前发布的 0 是错的，扣留至少不再发布错数。

## 全部 40 份保存的 8.01（`census.py` → `census.json`）

逐份读过并判断（前 23 份见 `../e01-keyword-branch/eight-o-one-judgements.json`，其余 17 份见本目录 `more-eight-o-one-judgements.json`）：

- **5 份**报告了注册人参与的并购交易：Pfizer 完成收购 Metsera；Paramount 前身同意出售、并完成出售所持 Viacom 18 的 13.01% 股权；Paramount Skydance 宣布收购 Warner Bros. Discovery 的提议、以及对其要约收购的修订延期。
- **其中 3 份正文里没有任何已声明关键词**（出售完成写的是 "sale ... equity interest"；提议写的是 "to acquire"——不含 "acquisition"；要约写的是 "tender offer"）。
- 字面关键词读法放行 7 份：对 2 份、错 5 份（Macy's 要约的 combined、Marriott 两份募资、Paramount 为规避 280G 加速归属里的 "Transaction Agreement"、Salesforce 回购的 "open market transactions"），并漏掉上面 3 份。
- 第一版决策包设想的"具名对手方"确定性模式只放行 Pfizer 1 份，漏 4 份（包括正文就叫 "the Transaction" 的 Viacom 18 出售协议）。

所以两个确定性候选都实现不了"经内容确认的并购公告"。这不是说确定性方法不可能，而是说这两条不行；采用内容确认口径，需要对**每一个** 1.01/2.01/8.01 条目做内容判断（不只关键词命中的那些），实现方式要么是在更多申报上验证过的更宽的确定性语法，要么是有具体调用申请的模型审阅。

## 原生 Run（`targeted-runs.json`，`compare_runs.py`）

在同步到本改动的运行树里跑 9 个 Run（七个窗口的 E01，加 Ford 的 C01、E05），闭包 `sha256:b4aebe32…`：全部冻结、出公共行、另一进程冷读得到同一 result_id，零调用。与 12 期间批次逐位置比 Run 记录：**Ford 的 C01、E05 的 result_id 与批次完全相同**——没有关键词条目的路线一个字节都没动；E01 四个发布值与批次相同（result_id 变了，因为观察记录现在绑定了所读的每个 8.01），Macy's、Marriott、Pfizer 由发布变为按名扣留。按新 Run 重新产出的 `../content-acceptance/e01-eight-o-one-read.json`：四个 MATCH（Lumen 新增，因为已不再由"别的条目替 8.01 确认"那种读法决定），三个扣留处阅读工具独立地也在 8.01 正文里看到关键词；接受登记 159→160。Pfizer 的坐标级缺陷只对这个扣留结果释放，发布过的 0 仍被撤回。

## 用例与注错

`tests/vnext/test_historical_event_items.py`（17 例）：40 份逐一唯一定位；与独立阅读逐份相同；复选框 "o" 不是引用；句末的条目引用不开始该条目；构造的引号标题、子条目标题、被隔开的重复标题、合并标题、签名收尾；关键词按目录的规范化与子串方式命中；把 `brief` 改成含关键词不改变任何结论；七个窗口的答案与扣留的命中集合；窗口内每个 8.01 都被读；观察记录绑定所读内容；只有直接条目的路线（C01、E05）不受影响。

九个注错全部被抓（`fault-injections.json`）：读 `brief` 而非正文、只读冻结窗口的前 300 字、一直读到文件末尾、有关键词也照发直接条数、任何小写词都算引用、引号不算引用、子条目各自成条、两次标题取第一个、直接条目路线也做正文读取。**如实记录两个只被构造用例抓到**：引号不算引用、两次标题取第一个——保存的申报里没有这两种形状。

## 不主张

E01 的正式匹配语义（待决定）；附件是否属于 8.01 正文；本帧其他期间（往年 8-K 正文未保存）；Paramount 与 JPMorgan 的 E01：前身 FY2024 的 Part III 修订现已按整段说明读成已批类别，事件窗口清除后 E01 因两份 8.01 正文带 "Transaction" 按名扣留（`../part-iii-statement-review/targeted-runs.json`）；继任 FY2025 仍缺 9 份前身 8-K 正文；JPMorgan 被期间选择与一份失败头文件挡住。零新增调用。
