# 定向运行：闭包 `30a7934b`（18 个位置）

## 跑了什么、为什么只跑这些

12 期间批次（闭包 `aa0eff7e`）早于三处改动：D02 斜体事项标签修复、D04 接线、跨块来源发现。本轮在同一只读运行树（`514b41a3` 内容加六文件注册补丁）下只跑受这三处影响的位置，驱动是同目录 `targeted_runs.py`：

- **D02 的 Paramount 两期**：斜体修复从未原生跑过。
- **D04 的全部 12 个期间**：批次时尚未接线。
- **Marriott FY2023 在重分块记录根上**：本期 10-K 行被移进历史块的构造情形，走完整条链（B01、B04、B12、D02）。该根由本运行树自己的录制会话建出——来源检查点登记在创建它的那棵代码树的信任日志里，用仓库树建的根在这里每个位置都停在 `ORDINARY_SOURCE_UNREGISTERED_LEDGER`（首跑即如此，已按设计重建，而不是绕过）。

**这不是该闭包的全量验收**；12 期间批次仍是它自己闭包的证据，不改签、不混入。

## 执行字节

仓库树与运行树的 minted manifest 只差 12 个叶子，全部在六个注册补丁文件上；`new_rule_files` 一个不差。18 个位置全部记录同一个闭包。零调用。

## 结果（`rerun-results.json`）

- **D02 Paramount**：两期都冻结、出公共行、另一进程冷读一致。与批次逐行比较，**只增不减、保留行顺序不变**，增的正是修复普查点名的斜体短标签——FY2024 的 "Asbestos"、"Other"（16→18 行），FY2025 的 "Asbestos"（28→29 行）。两个坐标仍被 D02 关键词代理缺陷撤回，所以本轮不改变任何交付。FY2024 那次创建在 Run 冻结后被容器重启打断，另起目录重建得到**同一个 run_id 与 result_id**。
- **D04**：12 个期间全部按名停在 `HISTORICAL_SEMANTIC_ASSESSMENT_NOT_REGISTERED:LIVE`，零调用——路线在，缺的是登记评估这个输入。
- **重分块记录根**（`RECORDED_TEST_ONLY`）：4 个 Run 全部冻结、出公共行、冷读一致；B01 23,713,000,000、B04 3,083,000,000 与未改动的根在同一申报上的值相同；四个 Run 的记录与清单都点名新块 `CIK0001048286-submissions-003.json`，即申报确实是对着列出它的那个块被证明的。

## 不主张

该闭包的全量帧；D02 Paramount 两个值的内容正确（关键词代理那一块仍在）；任何 JPMorgan 或 Salesforce 往年的值——跨块发现只保证它们取得原件后不再停在发现处。
