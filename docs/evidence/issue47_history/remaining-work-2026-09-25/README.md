# #47 剩余工作：按"卡在哪里"分，而不是按指标数分

路线表（现在是 37 条已接线、B13 只接了定义排除的一侧、D03 未接线）说的不是剩余工作。这里按每一项实际被什么挡住来分，每项点名依据文件；数字都是测出来的。本文件在 2026-09-25 两次更新，第二次加入金融适用分支、D04 接线、C02 判读、B06 阅读与跨块来源集发现。

## 今天的底数

五年框架 1,950 个位置（10 家 × 39 指标 × 5 年）。已存原件、能尝试的期间 **12 个**（Marriott 3 年、Paramount 2 年、其余 7 家各最新 1 年、JPMorgan 0），即 **468 个位置**；其余 1,482 个位置卡在来源上（缺原件 1,287、期间未发现 117 全是 JPMorgan、元数据阻断 78 也是 JPMorgan）。

## 一、离线实现（不需要任何授权就能做）

| 项 | 现状 | 下一步 |
|---|---|---|
| **跨块来源集发现**（新测出） | 本路线与 accession、Company Facts、零 AI 报表路线都只在主 submissions 文档的 recent 块里发现目标申报；50 个目标期间里 6 个（JPMorgan FY2021–FY2024、Salesforce FY2022–FY2023）的目标行只在历史块里（`financial-route/target-row-blocks.json`） | 以目标行所在的块作清单（冻结路由器本就接受历史块），不改变任何 recent 块内的答案。今天没有"原件已存且行在历史块"的真实正例，**获取之前或同时做**；不做的话这 6 个期间取得原件后仍会停在发现处 |
| C02 选择器 | 十个值全部撤回：246 条摘录里 94 条在任何口径下都不是董事会构成（`c02-board-read/`） | 后继选择器；去掉 94 条的规则要先用 246 条判读校验，其余部分取决于下面第 6 项口径决定 |
| C02 代理修订分支 | 规则携带但本仓库没有任何 DEF 14A/A 能行使它 | 等材料 |
| B13 适用分支（Ford、Enphase） | 钉期来源已备好并与冻结构建器逐字节相同；普通路线的 B13 请求合同还在 #28 修订（基础请求的分类缺陷已登记，角色/相关性变体是 #28 尚未接受的离线候选） | 等 #28 接受合同后移植；现在移植等于把调用花在所有者都没接受的合同上 |
| D03 | 普通链路没有 D03 的原生 Run 路线可移植（`normal_run_v3` 只消费已登记的 B13/D04） | 属 #28；#47 不在另一个 Issue 之前发明它 |

本轮已完成、从上表移出：六个金融指标的适用分支（`financial-route/`：同一准备交给两个解析器，JPMorgan 最新年报上六个指标逐字段相等；今天没有位置到达，因为 JPMorgan 的期间选择停在保存目录不一致）；D04 历史接线（`semantic-route-wiring/`：没有登记评估时按名停在 `MODEL_REVIEW_NOT_EXECUTED`）；D02 斜体事项标签；前身年份进框架。

## 二、需要 owner 决定（每项已有证据、选项与推荐）

1. **E01 的 8.01 关键词确认怎么读**（`e01-keyword-branch/decision.json`）：只在融资语境出现的关键词算不算并购公告。影响 Pfizer（已登记缺陷）与 Lumen、Macy's、Marriott 的接受。推荐：只认指向具名交易的用法。
2. **B03 两个"总 D&A"概念数值不一致时取哪个**（`b03-depreciation-scope/finding.json`）：Salesforce 的链条首个概念只是固定资产折旧。推荐：两者不一致即按名扣留。
3. **Part III 修订对报表类输入**（`part-iii-statement-review/finding.json`）：影响 Paramount FY2025 的 6 个报表指标。推荐条件式清除（无财报声明在场且修订不带治理分类以外的原生事实）。
4. **Part III 修订的措辞**（`part-iii-statement-review/second-instance.json`）：前身 FY2024 那份 10-K/A 的说明段措辞不在已批模式内，影响 FY2024 的 14 个位置。
5. **D02 关键词代理**（`d02-content-read/keyword-proxy-decision.json`，已有决策包，上一版误列为待找判据的实现项）：11 份申报里关键词放进 24 块、19 块属实、5 块只是顺带提到诉讼（另有前身 2024 一块同形）。三个实测规则都不能只去掉错的："关键词在枚举里"被 Macy's 1065 证伪，"注册人作主语"丢 Southwest 1584/1585 与 Paramount 1900，"所在附注标题点名或有事项"去掉全部 5 块但丢 4 条批准来源本就要的披露。选项：A 维持撤回（推荐现在）；B 收窄到标题点名的附注（改变含义，不推荐）；C 对这 24 块做逐块语义审阅（新调用类型，需要模型额度）。
6. **C02 "委员会信息"的口径**（`c02-board-read/README.md`，新）：是否包括委员会职能与运作（19 条）、构成政策与流程（51 条）。推荐只收构成事实（82 条那一类）。不论选哪个，94 条都在外面。
7. **#47 的 SEC 获取许可**：上限 **1,352**（年报链与元数据 150、事件窗口 1,046、治理代理 36、预留 120；`acquisition-plan.json`），许可对象已由 `tools/propose_historical_allowance.py` 生成并过了真实验证器（`acquisition-wiring/proposed-allowance.json`）。注意第一节的跨块发现：不修它，JPMorgan 与 Salesforce 那 6 个期间取得原件后仍然不交付。
8. **#47 的模型调用**（B13/D03/D04）：需要两件事，**额度本身不够**——WB-3 调用控制器按 id 登记 Requirement 世代、不认 `issue_47_v1`，`tools/check_provider_egress.py` 固定传输调用方的确切集合；给两者加 `issue_47_v1` 是改安全边界，需要单独审阅（`semantic-route-wiring/README.md`）。

记录、不提议：E01 的 1.01 直接计入（七个窗口 13 个 1.01 里 11 个是借款协议）；B01 先取合同收入（Lumen、Macy's 的合同收入分别比其总收入低 8.6% 与 3.8%）；B06 除数取母公司股东权益（Paramount 有少数股东权益，11,693 而非 12,887）——三者都是已批定义本身的选择或其最直接的读法，写在各自接受的局限里。

## 三、外部来源

获取计划分类见 `acquisition-plan.json`。关键依赖：没有 A 类（150 次），1,287 个缺原件的位置一个也动不了；JPMorgan 的 68 个分片落地前，它的另外三个期间连依赖都量不出来。上限不含 JPMorgan 的事件窗口与语义路线的模型调用。

## 四、内容核对

159 条接受全部来自已提交代码产出的阅读（`reading-producers/`）：D01 12、B01/B04/B05/B08/B09 各 10、B02 9、C03 9、B03/B07/D02 各 8、C01 与 E02–E05 各 7、C04 6、B06 4、B10/B11/E01 各 3、B12 1。已登记内容缺陷 25 条：**16 条仍在撤回当前结果**——C02 十条、D02 关键词代理四条（Pfizer 2025 的范围阅读、Lumen、Paramount 2025、Paramount 2024）、E01 Pfizer、B03 Salesforce；1 条按结果编号点名一个已被替换的旧 Pfizer D02 结果（它只撤回那个旧结果）；其余 8 条已按修复后的结果编号与规则版本释放。全量数字待本版本的整帧批次（正在跑）。

## 不主张

这份清单没有穷尽"业务上还可能错在哪"；它只列出已经测到的阻塞与已经读出的问题。
