# #47 剩余工作：按"卡在哪里"分，而不是按指标数分

路线表说的不是剩余工作。这里按每一项实际被什么挡住来分，每项点名依据文件；数字都是测出来的。本文件 2026-09-25 建立并三次更新；**2026-09-26 第四次更新**按外部审阅（对 `0b7f1c9c`）的归类重排第二节：E01 与 C02 是产品含义问题，Part III 报表类清除是准入边界，SEC 额度与模型调用出口是资源与安全边界；B03、D02 的已知错误继续撤回是既有要求，不再列为需要批准的新决定。

## 今天的底数

五年框架 1,950 个位置（10 家 × 39 指标 × 5 年）。已存原件、能尝试的期间 **12 个**（Marriott 3 年、Paramount 2 年、其余 7 家各最新 1 年、JPMorgan 0），即 **468 个位置**；其余 1,482 个位置卡在来源上（缺原件 1,287、期间未发现 117 全是 JPMorgan、元数据阻断 78 也是 JPMorgan）。

## 一、离线实现（已授权，不需要新决定）

| 项 | 现状 | 下一步 |
|---|---|---|
| D02 关键词代理 | 三条实测确定性规则各在一份真实申报上失败（`d02-content-read/keyword-proxy-decision.json`）；四个位置撤回 | 修复责任保留。三条规则失败不等于所有确定性方法都不行；若走模型审阅，要具体的新调用申请，且必须同时覆盖漏选方向（关键词从未放进来的 Item 8 块），只审关键词放进来的 24 块证明不了集合完整 |
| C02 选择器 | 十个值全部撤回：246 条摘录里 94 条在任何口径下都不是董事会构成（`c02-board-read/`）。与口径无关的一部分已查（`core-fact-reach.json`）：董事人数与独立董事数十个位置都在值里；委员会信息 5 个完整、4 个部分、Pfizer 没有——代理材料把委员会页面排成"标题 / 'Chair: 某某' / 每名成员一块"，块级选择器一块都取不到 | 等第二节口径决定；**选定后先补读漏选方向**——82 条只是当前入选集合里的正例，不是完整预期；修复还要能读委员会页面结构，不只是减法 |
| C02 代理修订分支 | 规则携带但本仓库没有任何 DEF 14A/A 能行使它 | 等材料 |
| B13 适用分支（Ford、Enphase） | 钉期来源已备好并与冻结构建器逐字节相同；普通路线的 B13 请求合同还在 #28 修订 | 等 #28 接受合同后移植 |
| D03 | 普通链路没有 D03 的原生 Run 路线可移植 | 属 #28 |

第四次更新时完成并从上表移出：

- **E01 的 8.01 从正文确认**（`e01-item-text/`）：冻结匹配器拿关键词比程序自己写的说明句，8.01 分支一份都没读过——明确的实现错误，只在历史路线修。过渡规则只回答不依赖含义决定的部分：窗口里没有任何 8.01 正文带关键词时发布直接条数，否则按名扣留。定向 Run（闭包 `b4aebe32`）：Enphase 0、Ford 2、Lumen 7、Southwest 2 发布，Macy's、Marriott、Pfizer 按名扣留；Lumen 由此被接受。
- **Part III 排版与措辞识别**（`part-iii-statement-review/`）：前身 FY2024 那份 10-K/A 是被分类器在**排版**上拒绝的（三段说明被拆成 12 个块，上限 8）。整段读说明的规则文件把它读成已批类别——事件窗口清除、报表类不清除；八个反例、八个注错。定向原生 Run（闭包 `903b8c7f`）：C01=9、E02=0、E03=9、E04=0、E05=2 由扣留变为发布，E01 按名扣留待含义决定，B01、B02 仍扣留且结果编号不变；五个新值由前身自己的 22 份 8-K 头文件逐份计数、两种窗口读法都一致，接受登记 160→165。
- **SEC 许可的文字范围与执行范围对齐**（`acquisition-plan.json`、`acquisition-wiring/`）：许可按授予逐项列出，门禁只放行被某个授予覆盖的请求；两份最近一次失败的事件头文件归入 B 类；账本根路径提案由门禁检查；上限 1,354。
- **B03 按名扣留接入历史路线**（`b03-depreciation-scope/`）：落实"不得把小计当完整总额、无法证明时准确限制该指标"这条既有要求；候选全部一致时保留已批链条的选择。十个位置里只有 Salesforce FY2026 由发布 0.2295 变为按名扣留，其余逐位不变；普通路线不改。定向原生 Run（闭包 `3e0183f3`，`targeted-runs.json`）：Salesforce 按名扣留、它带的 B01 与批次逐字节相同；Enphase、Marriott 的 B03 与 B01 结果编号与 12 期间批次相同；三家冷读一致；缺陷已按这一扣留结果与闭包释放（已发布的旧值仍被撤回）。
- **模型出口的限定差异与离线验证**（`model-egress/`）：见第二节第 5 项。

## 二、需要所有者决定的（按性质分，每项有证据、选项与推荐）

### 产品含义（指标回答什么）

1. **E01 要做哪一种**（`e01-keyword-branch/decision.json` 的 `question_as_posed_2026_09_26`）："按既定申报条目规则生成的交易线索"，还是"经内容确认的并购公告"？审阅推荐后者。要说清的张力：只修 8.01 不是完整的并购识别——七个窗口 13 个 1.01 里 11 个是借款协议，已批定义让它们不经确认直接计入。选后者就要对每一个 1.01/2.01/8.01 条目做内容判断；两个已量过的确定性候选都做不到（字面关键词：对 2、错 5、漏 3；具名对手方：放行 1、漏 4），实现方式要么是在更多申报上验证过的更宽语法，要么是有具体调用申请的模型审阅。"必须出现具名对手方"只是实现候选，不是业务定义。Part III 修好后又多一个等这个决定的位置：Paramount 前身 FY2024（两种口径下分别是 4 与 3）。
2. **C02 "委员会信息"的口径**（`c02-board-read/README.md`）：推荐只收构成事实——含委员会的设置、成员、主席以及相关的独立性与资格认定。不论选哪个，94 条都在外面；选定后必须补读漏选方向。与口径无关的那部分已先量（`core-fact-reach.json`）：两个数十个位置都在值里，委员会信息在 Pfizer 完全没有、另四家部分——所以选 1 的实现成本包括读委员会页面结构，不只是去掉 94 条。

### 准入边界（接受哪些输入）

3. **两份 Part III 修订能否各自清除本期报表类输入**（`part-iii-statement-review/statement-values-request.json`）：当前被拒 **14 个位置**（前身 FY2024 8 个、继任 FY2025 6 个）。每份修订的证据都分别从字节读出：点名的原报告（年度截止日与原始申报日与原件一致）、说明每一句、只含 Part III 与 Part IV、第 15 项只有 31/101/104、"未附财务报表"声明、原生事实只有 ecd 治理分类。推荐：有证据约束的**逐份**准入，不是让以后所有 Part III 修订凭两个机械条件自动清除；不能只凭没有财务标签就推出别的披露没变。九个 `not_covered_metric_ids` 无论如何仍拒。

### 资源与安全边界

4. **#47 的 SEC 获取许可**（`acquisition-plan.json`、`acquisition-wiring/proposed-allowance.json`）：上限 **1,354**（年报链与元数据 150、事件窗口 1,048、治理代理 36、预留 120），按授予逐项列出，账本根提案 `/Users/lyuhongwang/.local/state/sec_metrics/issue47-historical-sec-v1`。提案过了真实验证器，声明框架里没有一行被拒。仍是提案，授权前不发真实请求。JPMorgan FY2021–FY2024 的事件窗口在其分片到位前不可声明、不在上限内。
5. **#47 的模型调用出口**（`model-egress/`）：**先独立安全审阅，再谈调用申请**。三道门（控制器按世代登记、适配器按请求类型放行、出口扫描器固定调用方集合）都被此前世代按字节记录；补丁对三者共增 17 行、删 1 行，另加一个 300 行的出口模块与离线验证套件；许可、请求、计划、账本在仓库里的 `historical_model_calls.py`（不碰出口，扫描器通过）。验证在打了补丁的临时副本里用受控连接器完成：哪些调用、绑定什么、计数/停止/恢复、拒绝错误世代与绕过，结果封存在 `offline-verification.json`。**应用补丁的代价要决定**：三个边界文件被 #28 的世代按字节记录，补丁不能原样应用在 #28 仍在用它们的同一检出里——推荐只在 #47 的运行树里应用（与注册补丁同样的隔离）。审阅之后才是具体的调用申请：今天能量的往年 D04 请求 16 个（Marriott 2023、2024 各 4，Paramount 前身 FY2024 8）；最新年份那里的问题不是额度，而是要不要复审 #28 已在审的内容。

## 三、不是新决定：既有要求下的撤回（修复责任保留）

已知错误的值不交付是既有要求，登记表按结果与版本撤回，修复责任在执行者：B03 Salesforce（链条首个概念只是固定资产折旧；历史路线现已按名扣留，已发布的旧值仍被撤回）、D02 关键词代理四处（Pfizer 2025、Lumen、Paramount 2025、Paramount 前身 2024）、C02 十个值、E01 Pfizer（12 期间批次发布的 0；新路线按名扣留）。

记录、不提议：B01 先取合同收入（Lumen、Macy's 的合同收入分别比其总收入低 8.6% 与 3.8%）；B06 除数取母公司股东权益（Paramount 11,693 而非 12,887）——两者都是已批定义本身的选择或其最直接的读法，写在各自接受的局限里。E01 的 1.01 直接计入已并入第二节第 1 项。

## 四、基线带来的共享增量（`[shared-with-#47]`）

合并 `task/b06-new-source`（到 `9552226d`）带来六个标注与 #47 共享的提交，逐个量过对本帧的影响：

- **C04 登记表单路线**（`c04_registration_successor.py`，Spec v3）：普通路线里是**显式选择**才进入的后继，默认两形式路线原样；对 Paramount FY2025 给出 `C04_COMPARABLE_AUDITOR_FACTS_MISSING`（没有同 CIK 前期年报可比审计师事实），仍扣留、不给 0 或 1。本帧 Paramount 的两个 C04 位置在 12 期间批次里本来就是按名扣留（`HISTORICAL_GOVERNANCE_SOURCE_ROUTE_UNRESOLVED`），所以移植**不移动任何值**，只会让理由更精确。**暂不移植**：普通自动更新尚未默认选择它，"同 CIK 前期缺失时能否走另一条否定路径"在 #28 那边仍是业务决定；两者任一落定再移植。
- **B13 两阶段审阅**（`capacity_two_stage.py` 等）：只在记录模式下可执行、真实调用未授权；#47 的 B13 适用分支仍等 #28 接受请求合同。

## 五、外部来源

获取计划分类见 `acquisition-plan.json`。没有 A 类（150 次），1,287 个缺原件的位置一个也动不了；JPMorgan 的 68 个分片落地前，它的另外三个期间连依赖都量不出来。上限不含 JPMorgan 往年的事件窗口与语义路线的模型调用。

## 六、内容核对

12 期间批次（closure `sha256:aa0eff7e…`，`native-run-batch-12-periods/`）：442 个位置、440 个冻结 Run 与公共行、2 个失败（Marriott 2023/2024 C02，代理未保存）；178 个值里 159 个被独立读过、16 个被开放缺陷撤回、3 个没人读过（E01 的 Marriott、Lumen、Macy's）。其后 E01 定向 Run（闭包 `b4aebe32`，`e01-item-text/targeted-runs.json`）：Lumen 的 7 被独立阅读接受，Marriott、Macy's 改为按名扣留（没有值可读）。Part III 定向 Run（闭包 `903b8c7f`）：Paramount 前身 FY2024 的五个事件新值全部读过并接受。B03 定向 Run（闭包 `3e0183f3`）：Salesforce 改为按名扣留。接受登记 **165** 条，全部来自已提交代码产出的阅读（`reading-producers/`）。定向 Run 不是全量帧，12 期间批次不改签。

## 不主张

这份清单没有穷尽"业务上还可能错在哪"；它只列出已经测到的阻塞与已经读出的问题。CI 里被取消的作业只说明那次验证没有完成，不是通过也不是失败。
