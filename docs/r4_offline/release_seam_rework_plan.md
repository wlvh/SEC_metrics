# PR30 narrow successor release seam rework

Rejected as final approval: head19847e1f3f2d666b131422cd93f0c1230ef4c19e,
treef5e113889c973352b9ce3d27d30e2c4af5d17e37, v2 closure
sha256:ae1cd0cc3c59ae6ad7ef099d6661b5ec7604f7b385fedcbab41b2d7dd6df9bb3.
The accepted scoped invocation/acceptance/Run seam is retained. This rework
adds only the downstream release/projection composition and its tests.

## Reproduced boundary

The real Projector _release_context rejects issue_28_r4_scoped_engine_v2 with
"Issue #15 projection ReleasePlan is invalid", caused by "Requested ReleasePlan
is unavailable". Its Spec loader reads catalog/metrics, not catalog/r4_v2.
The actual public R3 matrix has four numeric JPM anchors and NOT_EXTRACTED
A09/A13; no nonfinancial R4 rows exist. New vNext coordinates are6 applicable
production results plus54 structural coordinates, not all15 qualification Runs.

## Pre-edit closure map

- A — R1–R3/Issue15/ai_first snapshots, publications, root mirrors, request
  ledger/acquisitions and PR22 archive: unchanged. No source reacquisition.
- B — activated issue_28_v1 and retained V1/V2 engines: unchanged.
- C — candidate execution closure: add private R4 release/projection/publication
  modules, typed manifest support and minimal dispatch to existing publication
  primitives. Regenerate only NOT_ACTIVATED v2/current proposal artifacts after
  code stabilizes. New files/dependencies must enter v2 execution authority.
- D — additive projection presentation data, narrow negative suites, full
  isolated recorded release rehearsal and a new benchmark receipt/log. Keep
  previously committed measurements historical and byte-identical.

## Required private interfaces

R4ReleaseContext binds the exact successor ReleasePlan, six real R4 Specs/task
closures, full registry/applicability, immutable R3 predecessor, aggregate
qualification and independently replayed fifteen-terminal graph. LIVE additionally
requires exact transition/merged implementation/live owner receipt authority.
Only the six production values enter projection; alternate/stability/fixture
issuers remain qualification evidence. Native Calculator/Run semantics produce
the54 N_A_STRUCTURAL results without source/AI fabrication.

R4 projection is an additive adapter over existing lower-level rendering and
compatibility primitives. The legacy Issue15 public Projector contract is not
widened into a caller-selected authority map. A09/A13 new coverage and R4 method
metadata are explicitly classified; untouched public coordinates and four
numeric anchors remain exact. No false retirement or legacy-anchor claim.

The new explicit R4 PublicationManifest distinguishes LIVE from recorded
rehearsal credit. Read-back carries its own immutable nested authority root
with the original R3 pointer/chain, all qualification bytes and source inputs;
it cannot depend on the later mutable active pointer still being R3. Existing
regular-file/hash checks and locked CAS/mirror/switch-intent primitives are
reused. Rehearsal capability cannot target the implementation checkout or
acquire real publication/qualification credit. Real switches need future owner
authorization as well as the exact live eligibility closure.

PR30 stays Draft. No activation, Ready, merge, PR-C, provider/paid/SEC egress,
real publication switch, production freeze, Stage-A or rollback is performed.
All new publish/rollback/restore exercises take place only in an isolated
recorded workspace and retain NONE_RECORDED_REHEARSAL credit.
