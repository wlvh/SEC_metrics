# PR30 dormant execution seam — development checkpoint, not approval candidate

PR30 remains Draft. `issue_28_v2` remains NOT_ACTIVATED. No Ready, merge,
PR-C, live cycle, production freeze, Stage-A, R4 publication or new SEC
acquisition is authorized or performed.

## Implemented

- Explicit repository-built live-scoped request, pending plan and private
  authorization types; offline evidence cannot be relabelled live.
- Socket-adjacent successor policy/request rebuilding through the original
  reservation-owned provider boundary. R4 keeps an exact 200000 context ceiling
  even though the provider technical capacity is higher.
- Native source-bound acceptance, raw-wire journaling, retry zero, no response
  reuse and direct-entry prefix gates for failed/UNKNOWN/incomplete terminals.
- Nine base requests plus three risk-selected stability entries; three native
  structured-primary Runs and four model-zero classifications.
- Native scoped/structured Run persistence, checker/calculator/replay and
  portable authority closure. Original V1/V2 engines and R1–R3 bytes are unchanged.

Current development proposal closure:
`sha256:deb3df24ad61abcb9c03cc28c802ce420330e23c3d0263986fb93c3b2bcfcc8b`.
It binds 102 execution files; the corpus was mechanically regenerated as
`sha256:f5a1a652865dc5d8d58fa82b880593c2ab08a55c3103dce3a6667de80721f385`.
Neither identity is offered for activation. Read exact commit/tree from Git;
embedding a commit's own hash into this committed document would be circular.

## Observed development evidence

- Fast suite expanded without removals: 29/29 PASS, per-entry timeout unchanged
  at 30 seconds. A concurrent-development run took 16.465 seconds.
- A provisional isolated 15-Run integration passed all four then-existing tests:
  twelve recorded scoped executions, three native structured Runs, full-record
  rebound mutations and fresh-process portable replay; provider opener count 0.
- That test used provisional closure `4d467e9a...`, before the last exact-summary,
  ancestor-path and complete-Run crash-gap fixes. Additional focused tests passed,
  but final expanded integration has not yet been claimed for this checkpoint.
- Historical R3 → exact R2/R1 and all 14 mirrors: PASS, 60.437 seconds under
  concurrent correctness work. Ordinary path-aware whitespace check: PASS.
- The host slept during the large development test: unittest active elapsed
  1936.859 seconds versus external wall 42535.38 seconds. This is not a benchmark.
  Exact commands, the earlier failure and timing caveat are retained in
  `live_seam_validation_attempts.json`.
- Rework provider/paid/SEC = 0/0/0. Whole PR-B remains 0/0/2; no filing reacquired.

## Remaining before a new review candidate

1. The actual Issue28 `APPROVE_SEC_CONTACT_AUTHORITY` owner comment is absent.
   It must be published/captured with real URL, author, time and body SHA, then
   represented as a typed policy Decision. Do not fabricate owner provenance.
2. Bind the final policy/implementation closure and refresh its current proposal
   ReleasePlan/corpus, without editing any activated or historical authority.
3. Run the expanded final recorded/portable integration and complete targeted,
   historical and publication regressions against that final source.
4. Rerun the same complete offline benchmark and exactly one final independent
   offline disk replay, requiring at least 10x. The prior 21.789879x result stays
   historical `f415859` evidence; it is not current-head performance acceptance.
5. Final exact-head CI, semantic/call-graph/capability/path gates and independent
   review. The next owner activation is a transition decision, not a live grant.
