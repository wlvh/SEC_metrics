# PR30 successor release implementation — review evidence

The R4 qualification output now reaches a repository-owned successor release
context, native public projection and the existing immutable publication
transaction. The complete recorded rehearsal passes. Real active publication
remains R3; all new rehearsal evidence has no qualification or publication credit.

## Candidate authority

- Base main: `c45338567700e3048f4cf32d251369e4521e9444`.
- Requirement: `issue_28_v2`, closure `sha256:5b7a386b7c95f8b9542a2251a94ec8d98876e7c833d49132364c77024b27ff9e`, state `NOT_ACTIVATED`.
- Successor ReleasePlan: `config/release_plans/issue_28_r4_scoped_engine_v2.json`.
- Added metrics: `A03 A04 A09 A11 A12 A13`; B06/B13 stay outside R4.
- The policy-content Decisions and 622 parent fragment dispositions are unchanged.
  The closure update binds the release modules, their complete production scanner
  census and historical data dependencies. Activated v1, retained engines,
  Issue15 and R1–R3 bytes are unchanged.
- Exact final head/tree and CI are in the PR body; embedding the containing
  commit's own SHA in a committed receipt would be self-referential.

## Execution and publication boundaries

`r4_release.py` validates the real successor plan, six `catalog/r4_v2` Spec/task
closures, ten-company registry/applicability and exact R3 predecessor. A LIVE
context additionally requires v2 transition activation, the actual approved
merge ancestry and unchanged implementation, pending plan, owner live receipt,
complete LIVE summary, twelve scoped and three structured terminals, usage,
no reuse and independent `EXACT_PLAN_LIVE_QUALIFICATION_ONLY` replay.

`r4_projection.py` selects only production ordinals, loads their native Results
and creates 54 zero-source structural terminals through Calculator/Run freeze
and replay. The result set is exactly six applicable +54 structural =60 delta,
300 cumulative. The renderer produces 381 matrix rows after preserving legacy
non-R4 rows. Four historical numeric anchors and two approved native backfills
pass strict compatibility; A13 uses the approved international net revenue Spec.

`r4_publication.py` carries the original R3 authority root, exact qualification
graph and native structural Runs inside a typed `R4_SUCCESSOR_PUBLICATION_MANIFEST`.
The shared publication module retains its regular-file/exact-set/hash gates,
locked CAS and switch-intent recovery. The existing Issue15 Projector contract
is unchanged. The only shared hooks dispatch explicit R4 artifacts to their
own verifier and switch capability.

Recorded artifacts cannot satisfy LIVE staging or real switch authorization.
The private rehearsal factory refuses the implementation checkout, its ancestors,
descendants and Git copies. Its immutable capability preserves
`NONE_RECORDED_REHEARSAL`. The CLI exposes no recorded/rehearsal override.
Real publish/rollback/restore re-read genuine owner comments bound to current
head/tree, publication, release receipt and R3 edge; transition, live execution
and release authorization remain distinct evidence.

## Recorded release rehearsal

`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.vnext.test_r4_live_qualification -v`
passed 7/7 in 3496.393s (rc=0). The generated complete evidence receipt is
`sha256:f5452b9643e0e8c928ddaf1f5c296102acc2b8345cc5b1d752d4cd8e064f16f0` in [the rehearsal artifact](release_seam_rehearsal_evidence.json).
The new process imports frozen bundle code while the OS denies original
checkout reads, bundle writes and IP networking. The local Docker Unix socket
is available only to run the existing network-none parser worker.

| Object | Exact identity |
|---|---|
| release_context_id | `sha256:ed0f4cfe4562ece5b11677a08a06cf38b71625d53ee75faedd1e67e5854f10a1` |
| publication_id | `publication_06d0e56fd3d9af97b9bd5e3aaf8b8183a3089f51b18bcc8ffd1a059282208964` |
| release_receipt_id | `sha256:27e813c86d39184a281e95745e09d21b1abb1832f101a8d41a62570102d02e2b` |
| batch_manifest_id | `sha256:7f4157e6cc1873139aa14448ca0254f3fad2d973a35ae49b77d9a9eca7d50d74` |
| publish_receipt_id | `sha256:db1ae05c514b85fbc09171209390eb56c0789a137a52aae2165e55a39c1203dc` |
| rollback_receipt_id | `sha256:3cd81e0f51519047afb713364b8cfa6bccf74f6e209b430d74f7286ed3c1a5ec` |
| restore_receipt_id | `sha256:6714fa4f623bf1adf938cd792fadba7ce2eb48c87a26bb052b13e0fca679f88d` |
| crash_recovery_receipt_id | `sha256:224baa362d7ce29e6cace7e5a67aa35e8be50f7debcc90d670666a3dec38e828` |
| post_pointer_crash_recovery_receipt_id | `sha256:1f0977c357ce6e1d565f0a89707d429eccdb754a437ec0e2ecd53bcded6bb605` |
| active_terminal_receipt_id | `sha256:ab63d118a3d0173d6465370d50eaa9a27dd7c56f2b266043dd4b034f9c18389b` |

The temporary transaction exercises publish, R4→R3 rollback, R3→the same R4
restore, interrupted mirror writes, pointer-before-switch-receipt recovery,
pointer/mirror tampering and the final 14-mirror terminal. Real root bytes stay
identical throughout. The final aggregate, public manifest and every switch
receipt retain `NONE_RECORDED_REHEARSAL`; this is implementation evidence.

## Negative coverage

Actual ReleasePlans reject Issue15 substitution, wrong Requirement/closure,
wrong R3 parent and wrong added metric set. Old A13 Spec substitution fails
execution-authority validation. Complete native graph tests reject missing,
extra or duplicate production selection, alternate/stability selection, BAC/Citi
fixture coordinates, post-replay terminal mutation, plan/Spec byte drift and
recorded-to-LIVE promotion. Native grid tests reject missing/extra/duplicate
structural coordinates and false applicability. Frozen structural Runs contain
no sources, observations or AI attempts, and read-only replay cannot create one.

Typed manifest tests reject missing identity/credit, historical marker grafts,
subtype downgrade, extra/missing files, symlinks and changed public bytes before
semantic acceptance. Owner/activation/merge tests reject forged or retargeted
receipts, wrong head/tree/closure, unmerged ancestry and changed production bytes.
The accepted live seam retains usage/UNKNOWN/no-retry/no-reuse and OPEN/FROZEN
recovery negatives. [The validation ledger](release_seam_validation_results.json)
contains exact commands, return codes, durations and stdout/stderr evidence.

## Validation and retained limitations

Targeted 226/226, additional cold/substitution 2/2, full historical 231/231,
recorded execution/release 7/7 and fast 32/32 pass. Post-benchmark checks include
the six receipt tests, formal R3/R2/R1 and14mirrors, arbitrary-token financial
negative, provider call graph, semantic/scalability and path-aware whitespace.
The standalone counts overlap. GitHub fast CI is separately verified at the
final pushed head. Twelve unchanged legacy uncontrolled-adapter tests remain
`PRE_EXISTING_STALE_TEST_DEBT`, never included in PASS or a whole-repository claim.

The full 21.308919× [benchmark report](performance_release_seam_results.md)
charges its unique fresh offline replay equally to both alternatives. This
16-case performance replay is separate from the fifteen-Run qualification
replay and publication rehearsal. Prior benchmark receipts/logs remain exact
historical bytes. The first attempted measurement was invalidated by a300s
sleep/clock gap and is retained with zero performance credit.

Final whitespace checks use the verified GitHub base SHA explicitly because the
local `main` ref was older. Source/config/catalog/tests/ordinary documentation
pass. `release_seam_rehearsal_evidence.json` is an exact copy of native generated
content-addressed evidence and retains its final blank line (generic whitespace
N/A); its SHA-256 is
`73ebc072b181d6a72235dc726f25d143401fd88c203dca5ba649940cf0f288bf`.
Its self-ID, full recorded read-back and original generated bytes all pass.

Two implementation defects found during rehearsal were corrected: R3's portable
ReleasePlan path differs from R2's wrapper path; and cold loading needed the
historical root ReleasePlan plus four foundation receipts. A cheap cold scanner/
authority test now covers the latter. Intermediate test-harness failures are
also retained in the ledger; no production validation was loosened to hide them.

[The preserved-boundary proof](release_seam_preservation.json) records unchanged
2,396 historical files, accepted scoped execution/source/semantic/parser paths,
both acquisitions and ledger983. Rework provider/paid/SEC is0/0/0; whole PR-B is
0/0/2. PR22 remains archived with no credit/reuse. PR30 stays Draft, v2 remains
NOT_ACTIVATED, and the next gate is independent review of this candidate.
The [committed PR-C commands](pr_c_release_entrypoints.md) need no new Python.
