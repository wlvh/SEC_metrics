# Issue #28 v8 — limited annual continuity

One reviewed implementation, unchanged rule and expiring isolated stage permit
may consume different complete ordinary annual inputs. Each input still binds
its raw SEC attempts, real period and issuer, native B01/B10 Runs, original
request/response/usage, accepted results, whole package and exact predecessor.
Discovery, complete successful candidate and committed publication are separate.
Pending publication is resumed without another model extraction. Partial or
uncertain results never replace the last complete version. Original B03 records
and inherited coordinates retain their own periods and evidence boundaries.

The current task delegates this rule's bounded content only. A real unedited
repository-owner stage comment, independent implementation review, at most seven
days validity, fixed external roots and permanent cumulative budget are required
before business egress or an isolated switch. Provider/paid maximum is 3/3: two
normal distinct annual inputs and one conditional repaired failure verification;
SEC maximum 6, no retries. Unknown counters stop additional calls. Delivered or
exhausted stages cannot restart through another input/head/directory. No stage
can write the actual production root or start a permanent production scheduler.

B01 Revenue and B10 worldwide/comparable/systemwide semantics remain unchanged.
The native source locator, label representation and ownership, Evidence, SYSTEM
Review, Calculator and original transport/usage requirements remain mandatory.
No generator PASS flag, local fabricated permission, inherited qualification
credit or per-input answer table supplies acceptance. Independent source review
of the two known historical inputs is stage acceptance evidence, not a claim of
universal unseen-source correctness. New input hashes do not change this rule.

An isolated historical seed may use real original B01 results and originally
committed qualification B10 evidence via existing read-only replay. It is a new
test seed, never a claim that production ran in that historical year. The seed
may replace the selected two coordinates with their actual older periods; other
rows retain their own periods. Ordinary later updates never regress periods.
Historical submissions visibility is explicitly simulated over unchanged raw
SEC bytes, with a derived receipt; no fake SEC request or edited raw file.

Complete packages share the existing Projector, PublicationView and native
pointer/intent/receipt mechanisms. Every switch pins its actual predecessor;
recover only completes or undoes that same reserved transaction. Production
activation and long-term permission remain outside this work package. Old v1/v2
rules, Requirements, failed/successful Runs and publication packages are immutable.
After continuity, repeat/failure/budget/trigger acceptance, stop this pilot and
hand off to bounded R5 migration with per-batch old producer/repair retirement.

Owner update, 2026-09-10: overwrite the current model configuration with DeepSeek-V4.1-Flash (API model deepseek-flash), without a new policy or Requirement version. For ordinary continuity the execution-bound config/provider_model_runtime.json selects the model within the existing approved provider and API. Request generation, adapter, controller and Run replay use the same exact selection. This supersedes the carried model name for new continuity inputs only; no response identity mismatch is accepted as an alias. Existing scope, prompt/schema, 200000 input-usage ceiling, retries and call limits remain. No new calls or reopening of the already closed stage follow from this configuration edit.
