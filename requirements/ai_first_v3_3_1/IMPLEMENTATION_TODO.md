# Issue #12 实施清单

本清单把 Contract Revision R2 的 Spec Unit 映射到代码、测试、文档与完成证据。状态仅在证据真实产生后更新：`[ ]` 未完成，`[~]` 进行中，`[x]` 已完成，`[!]` 外部阻塞。

| SU | 状态 | 代码改动 | 测试与证据 | 文档与契约 |
|---|---|---|---|---|
| SU-00 Requirement / baseline | [!] | exact FSD、live Issue body、Decision Register、`c37cecd` clean baseline manifest 与旧 lodging/B03 inventory 已保存；Requirement/catalog 已纳入 source closure | Requirement exact hash、Decision 单链、byte drift、baseline artifact schema/order/anchors 回归已实现 | 文档已同步；仅 D-01 仍需外部 HUMAN provider/egress 批准 |
| SU-01 schema / canonical / states | [x] | `canonical.py`、`records.py`、`states.py` 已实现 strict JSON、NFC、ordered/set、Decimal、跨解释器扩展日期/UTC 格式、递增的 calculator/spec/canonical semantic versions 与独立状态；STARTED attempt 仅属 OPEN 工作过程，terminal Run gate 要求 SUCCEEDED/FAILED | `test_canonical_hashes.py`、`test_state_model.py`、STARTED freeze 负例、Python 3.9/default syntax/format compatibility | `architecture.md`、`AGENTS.md`、capability 已同步 |
| SU-02 source / table-grid / reader input | [~] | `sources.py`、`table_grid.py`、`reader_input.py` 已实现 portable source、多 observation identity、完整 table-grid、exact manifest、raw-byte replay 与 locator round-trip；`resource_limits.py` 集中约束 untrusted HTML/table/span/entity/text/expanded-cell 资源；Company Facts adapter 绑定 raw hash、registry CIK、官方 URL、accession 与 fact identity | source/table/reader-input 正负例已实现；span/entity 超长数字在 Python 3.9 大整数转换前失败，其余 span/table/text/filing-total 超限在物化前稳定失败；freeze 从 RawBlob bytes 重建 table-grid 和 structured fact set，业务词不改变 table set | architecture/provenance/source policy 已同步；request-ledger prefix/membership adapter 仍待 full staging |
| SU-03 MetricSpec / generic DSL | [x] | `catalog/`、`specs.py`、`constraints.py` 已展开 defaults、selection policy、ordered DSL、guards、identity、quality、projection；company traits 只从 registry/profile 确定性投影 | semantic/closure/order、unknown、cardinality、cycle、depth 32/node 256、Decimal boundary，以及入口/freeze traits 反注入已实现 | 指标定义、capability/interact 与 METH-01 已同步 |
| SU-04 AI adapter / Reader | [!] | `create_review_run` 只接收 disclosure locator/source facts/adapter 并从 repository/RawBlob 派生 compiled Spec closure、Requirement hashes、derived URI 与 temperature；provider-neutral recorded adapter、从模块固定 repository root 的 Requirement closure/effective D-01 编译的 remote gate、三角色 Reader，以及 request/task/raw-response content-addressed 留痕已实现；`run_ai_attempt` 只接受 adapter、`PreparedReaderRequest` 与 clock，重验 table/system/task binding，固定 temperature=0 与严格 response validator，不接受 caller sampling mapping/callback；该对象降低误用自由度但不是安全边界，freeze/load 仍独立重放持久化 bytes；caller policy/root/transport 均不可注入，十字段 policy 只交给仓库 factory，每次 outbound 前重验当前 policy/closure，attempt 从 `TransportObservation` 记录实际 host/region/timeout/retry/payload 与 egress fact | workflow repository derivation、no-op validator/sampling 入口删除、无 Candidate 引用非法 SUCCEEDED response 拒绝；pending/构造后撤回 D-01 均 transport=0、伪造 DTO/替代 root/transport 拒绝、fixed authority APPROVED snapshot、exact-policy preflight、payload no-egress、actual-host mismatch、带 observation timeout FAILED 与缺 observation fail-hard 通过；remote live 3-run NOT_RUN | 双网络边界与非强沙箱声明已同步；D-01 仍 PENDING，且仓库尚无 live provider factory，因此 live 继续阻塞 |
| SU-05 Evidence / safe renderer | [x] | Evidence 只重读 locator/cell/local labels；generic constraints 与完整表安全 renderer 已实现；超长 cell 用不可见 HTML-comment 分行完整保留，总 review bytes 超限失败且不裁剪 | evidence wrong-cell/local-label/1%/invalid AST、renderer adversarial/长行/总预算、semantic audit 已实现 | capability/review/untrusted text 文档已同步 |
| SU-06 Review / freeze / replay | [x] | whole-unit review、只接收真实 HUMAN 选择并从 ReviewUnit 派生 claims 的最小 CLI、Decision 单链；REJECT-empty 与 APPROVE-exact 语义由 append/finalizer/freeze/replay 重放；每个 ReviewUnit 必须有 effective HUMAN decision，Observation roles、consumption 与 Result/Trace exact-complete；workflow/finalizer 不接收 traits；Run 拒绝 fiscal-year/日期矛盾；PASSED/FAILED/NOT_RUN 可 freeze audit，但只有 PASSED 可进 publication；freeze 要求所有 attempt terminal，重放每条 SUCCEEDED response，并拒绝 missing-required-source+PUBLISHED；全 WITHHELD 的 missing-source audit Run 仍可冻结；其余 registry/AI/Observation/Calculator/Result/Trace/receipt 重放保持 | CLI APPROVE/REJECT claims 派生、STARTED、未引用非法 SUCCEEDED response、missing-source PUBLISHED 均失败；missing-source WITHHELD、三种 validation receipt 与既有 trait/graph/period/identity/unit/value/formula/scope/quality/applicability/reason/raw replay/receipt/bytes/socket 正负例已实现 | architecture/interact/SOP/TESTING/capability 已同步；权威 required-source plan 尚未建模，当前先严格消费 manifest 的非空缺失声明；primitive 按单 Run 单写者使用 |
| SU-07 B01 + lodging shadow | [!] | B01 `preserve_reported`、B03 unit guard、B10 percent→ratio、B11/ADR exact-USD-or-WITHHELD；non-lodging 在 source/AI 前短路但仍从仓库 Spec 持久化 N_A Run/Result/Trace；recorded lodging/non-lodging/1% identity 已实现 | B01 EUR 保留、B03 mixed unit 与 AI EUR fail-closed 通过；non-lodging 断言 AI/SEC=0、Run 可 freeze/replay 且 N_A Result/Trace exact set；第二真实 filing、独立第三 holdout、live 三轮和完整 B10/B11 parity 未完成 | D-05 supersedes 记录与 02 指标定义已同步；缺口不能写成泛化/Cutover 证明 |
| SU-08 Spec-driven B03 | [~] | generic role resolver/calculator/Trace 已由 compiled Spec 驱动 B01 reuse、OI/D&A/cross-check/guards/quality；只有 accepted branch 提交 component IDs，freeze 从 Company Facts 重算 B03 自有组件及 B01 dependency Observation | 防双计、跨 accession、伪造 B01 dependency、0.99/1.00/1.01% calculate→freeze→replay、zero、Decimal context、Marriott/Pfizer legacy anchors，以及 Pfizer 真实 bytes `OK_APPROX` parity/FROZEN 已实现 | B03 Spec/指标定义/Trace 已同步；全 non-FI staging parity 与旧 active resolver 退出未完成 |
| SU-09 Projector / staging / pinned view | [~] | content-addressed BatchManifest 从 registry/display mapping、traits/applicability、release plan 与全部 PASSED FROZEN Runs 派生 complete company×metric set；Projector 先按 baseline schema/row count/size/hash 验证 legacy metrics/evidence/Golden，再从 batch Results/Traces/Observations 实际生成 metrics/evidence/compatibility/fixed repair bytes，并调用既有 row projection/reconciliation helpers；入口不接收 caller facts | 单公司/缺 metric/重复 key/locator 中间 symlink/Run tamper、schema-valid baseline 增行失败；scoped complete batch、row preservation、evidence exact/method delta、delete/add/value drift、non-lodging N/A、reviewed B10/B11 缺 form/filed 与 active view exact bytes 通过 | architecture/interact/TESTING/capability 已同步；真实十公司 staging parity、现行 report 切流与旧 producer 退出未完成 |
| SU-10 transaction / cutover / rollback | [!] | immutable exact bundle、bundle 内 Batch/Projection/result-row proof、生产 gate runner 生成 coverage/scalability/stratified/recorded validation/docs 并记录每项 execution evidence hash、prepare 二次重验、read-back persisted-proof+byte-integrity 边界、lock+CAS、pointer、mirrors/recovery、pointer-verified latest/active identity、单一 publication root、committed-predecessor rollback 已实现；detached `migrated_results`、自报 PASS/status/boolean 与独立 path 参数已删除 | arbitrary bytes/self-signed PASS/caller-authored repair/report PASS、missing execution evidence、row/evidence/nested-shape mutation、BLOCKED candidate、active Run 身份冲突、prepared-only sibling、锁边界 drift、tamper/concurrency/pinned view/recovery 均失败；complete batch→prepare→commit→view 通过 | 文档已同步；recorded Golden 由 frozen baseline+strict parity 证明，不冒充 active Stage 10/12 full 重跑；旧 producer 退出、active Cutover、故障注入 full flow 与真实 rollback/checker 仍阻塞 |
| SU-11 docs / acceptance | [!] | acceptance runner、symlink fail-closed semantic gate、shared SEC identity validator、README generator + stable post-processor parity 与 tracked 权威文档已实现/同步；full runner 将 live 00–11 与离线 Stage 12/checker 分开分类 | 空/null/畸形/example SEC identity 双入口失败；semantic scanner 对 token 泄漏与 symlink fail closed；本地修复记录已登记，远端 PR body/thread ledger 待明确发布授权；最终运行证据以本次发布记录为准 | 当前无 secret-consuming producer，canary 尚未穿过真实生成链路；active bundle provenance closure 尚未 Cutover；live/rollback 保持 NOT_RUN，full 在外部 gate 未满足时保持 BLOCKED |

## 2026-07-31 Review / 修复收口记录

| Finding | 历史检测 | 本轮跨入口收口 |
|---|---|---|
| D-01 transport authority | PR #10 已出现“声明权威但实现未执行”的同族问题；本轮独立复审又确认构造后撤回仍可 egress，且旧式无 observation result 会补造获批 host | 仓库 factory 所有 transport；每次 outbound 前重验当前 policy/closure；attempt 只记录实际 observation；异常/旧式结果缺 observation 不猜测；freeze/replay 重验 SUCCEEDED attempt |
| ReviewDecision 语义 | PR #13 R8 已关闭 ReviewUnit 入口但明确留下 Decision 自身入口；R15 已要求按不变量跨入口收口 | `REJECT-empty` 进入 record validator；`APPROVE-exact` 进入 binding validator；append、全 supersedes chain、finalizer、freeze/load/replay 共用 |
| table-grid / renderer 资源 | PR #13 R13 声称覆盖性能但未产生资源测试；本轮独立复审又确认 Python 3.9 会对超长 span/entity 十进制词法做近二次方大整数解析 | 集中预算覆盖 span/entity 词法、source/expanded cells、维度与文字；大整数转换及物化前失败；review 无损分行与总 bytes fail closed |
| freeze SOP 状态 | PR #9 已出现文档漂移同族问题 | SOP 使用结构化三状态表，并由 freeze/publication 场景测试与代码常量联合验收 |
| Run terminal/source authority | PR #13 R6/R8 的相邻对象绑定曾仅部分修复 | freeze/load 拒绝 STARTED、重放全部 SUCCEEDED response；missing role 与 PUBLISHED 不可并存，WITHHELD audit 保留；权威 required-source plan 仍诚实列为后续项 |
| Projector / publisher 双真相 | PR #13 R1/R2/R9/R15/R32/R33 多轮出现 caller 自洽 bytes、receipt 或局部 exact set 未绑定完整业务事实的同族问题 | content-addressed BatchManifest 绑定完整 PASSED FROZEN Run 集合；frozen baseline 绑定 legacy metrics/evidence/Golden exact bytes；Projector 生成 candidate 与 repair rows并保存 result-row proof；publisher 重跑 Projector/gates，拒绝任意 legacy/staging、自写 repair 与自签 PASS |
| Batch company×metric 完整性 | R33 修复 metric 子集后仍只检查单公司 Run 的 metric-name set，未建立 release company 维度 | expected set 从 registry、traits/applicability 与 release plan 派生；baseline 提供 legacy exact bytes/parity；缺公司、缺 N/A、重复/额外坐标与跨 period 混批失败 |
| non-lodging N/A 持久化 | workflow 的 trait 短路早于 `create_run`，现有测试只看返回字典，导致 Calculator 已有 N/A 分支无法进入 freeze/replay/project | source/AI 仍为零，但先从仓库 Spec 构造 Result/Trace，再创建 Run 并持久化；场景测试覆盖 Run 目录、exact records、freeze 与 replay |
| Publication 声明真实性 | Projector helpers 仅测试调用、任意 staging/自报 PASS 可 publish 时，architecture/TESTING/capability 仍写成 active verified release | tracked architecture/interact/TESTING/capability/SOP/本清单按真实 batch/gate/read-back 边界同步；远端 PR body 与 thread ledger 只在明确发布授权后更新 |
| Publication 路径与 latest 自报 | PR #13 R16/R17/R25 逐次修补 caller-supplied path/view | commit/rollback/recovery/view/status 统一从 publication root 派生；latest 只从 persisted Run/publication locator 推导状态与 boolean |
| SEC identity / canary | live 前置曾只排除少数 example email；canary 未进入 producer | acceptance 与 SecHttpClient 共用非空/格式/example validator；canary 目前只证明 scanner 检测能力，真实 producer 闭环留作 live 前硬门 |

本节记录本地实现事实；发布前把本表的新收口项与最终动态 receipt ID 同步到 ignored `PR_BODY.md` 的 Review / 修复记录。receipt ID 不写回本 tracked 状态表，避免验收回执与 source bytes 循环漂移。

## 外部 gate

- `[!] D-01`：尚无 provider/model/endpoint/region/retention/data-use/payload/filing-egress 的 HUMAN 批准。remote adapter 必须保持关闭；live 3-run shadow 与 live Cutover 不得被 recorded/local 结果冒充。
- live SEC 全批次还要求运行负责人把 `config/sec_config.json` 的示例联系人替换为有效 organization/contact email，并在干净隔离 checkout 执行。
- 生产 registry 只有一家 lodging 公司；第二真实 filing fixture 与实现冻结后由独立作者加入的第三 holdout 尚不存在，不能由同一 synthetic fixture 自证泛化。
- 旧 lodging/B03 producer 仍在现行 active call graph。没有 staging exact parity、old-resolver-throws full flow、active pointer 或 previous-bundle rollback→report→snapshot checker→restore 证据。
- 本 PR 只发布 recorded/shadow 代码与真实离线证据；不得把 PR 合并或 recorded receipt
  解释为 live/full/Cutover 授权或成功证明。
