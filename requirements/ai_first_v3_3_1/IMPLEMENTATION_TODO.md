# Issue #12 实施清单

本清单把 immutable R2 的 Spec Unit 映射到代码、测试、文档与完成证据，并叠加 exact R3 Addendum 明确列出的 superseding decisions。R3 不替代 R2 的 SU/AC 和详细契约。状态仅在证据真实产生后更新：`[ ]` 未完成，`[~]` 进行中，`[x]` 已完成，`[!]` 外部或运行证据阻塞。

| SU | 状态 | 代码改动 | 测试与证据 | 文档与契约 |
|---|---|---|---|---|
| SU-00 Requirement / baseline | [x] | exact FSD、immutable R2、exact R3 Addendum、历史可追溯且单链 effective 的 Decision Register、冻结 baseline/release/semantic versions与旧路径 inventory 已绑定 | R2/R3/FSD/Decision byte drift、R3 supersedes、baseline anchors 与 closure 回归已实现 | D-01 历史 pending 保留，R3 新 record形成唯一 effective `APPROVED`；当前 blocker是运行证据/secret，不是provider决策 |
| SU-01 schema / canonical / states | [x] | `canonical.py`、`records.py`、`states.py` 已实现 strict JSON、NFC、ordered/set、Decimal、跨解释器扩展日期/UTC 格式、递增的 calculator/spec/canonical semantic versions 与独立状态；Candidate新增assistant-output binding使canonicalizer version 2→3，source plan绑定latest verified immutable request attempt且live拒绝legacy locator使其3→4；当前semantic runtime versions hash为`sha256:394bbd29f4f8d67ef07a085c8e3e5c84db6c2ac22deee3e587f1e1a8684c59e8`，旧closure/approval/Run/Batch/publication失效；STARTED attempt仅属OPEN工作过程 | `test_canonical_hashes.py`、`test_state_model.py`、Candidate/attempt双hash篡改、STARTED freeze负例、Python 3.9/default compatibility | `architecture.md`、`TESTING.md`、capability 已同步 |
| SU-02 source / table-grid / reader input | [~] | `sources.py`、`table_grid.py`、`reader_input.py` 已实现 portable source、多 observation identity、完整 table-grid、exact manifest、raw-byte replay 与 locator round-trip；`resource_limits.py` 集中约束 untrusted HTML/table/span/entity/text/expanded-cell 资源；Company Facts adapter 绑定 raw hash、registry CIK、官方 URL、accession 与 fact identity；release input plan先验ledger manifest，再按exact source identity选择有序ledger中最后一个验证通过的immutable attempt并绑定attempt/body/header/class；recorded仅容许唯一显式legacy locator，formal live拒绝该class | source/table/reader-input 正负例已实现；span/entity 超长数字在 Python 3.9 大整数转换前失败，其余 span/table/text/filing-total 超限在物化前稳定失败；freeze 从 RawBlob bytes 重建 table-grid 和 structured fact set，业务词不改变 table set；latest immutable优先、claimed-immutable tamper、live legacy拒绝及publication membership负例验证真实 row、声明 locator 与 immutable body/header | architecture/provenance/source policy 已同步；request-ledger source-plan与最小已用 prefix/membership adapter 已完成 scoped recorded 收口，仍待真实十公司 full staging |
| SU-03 MetricSpec / generic DSL | [x] | `catalog/`、`specs.py`、`constraints.py` 已展开 defaults、selection policy、ordered DSL、guards、identity、quality、projection；company traits 只从 registry/profile 确定性投影 | semantic/closure/order、unknown、cardinality、cycle、depth 32/node 256、Decimal boundary，以及入口/freeze traits 反注入已实现 | 指标定义、capability/interact 与 METH-01 已同步 |
| SU-04 AI adapter / Reader | [!] | fixed OpenAI Responses transport已实现：`gpt-5.6-terra`、`api.openai.com`、`store=false`、strict schema、空tools、env key only；request/schema、structured assistant output与provider envelope分别content-addressed，Candidate绑定`assistant_output_sha256`，`raw_response_sha256`独立审计provider envelope；同一operator用recorded或live adapter，retry_count=2形成initial+2独立Run/attempt | authority/root/host/model/payload/timeout/schema/secret、Candidate/assistant-output/envelope错配负例、独立retry/exhaustion与三成功稳定性检查已实现；真实remote live三轮NOT_RUN | effective D-01已APPROVED；本轮`OPENAI_API_KEY` missing，不能产生live evidence |
| SU-05 Evidence / safe renderer | [x] | Evidence 只重读 locator/cell/local labels；generic constraints 与完整表安全 renderer 已实现；超长 cell 用不可见 HTML-comment 分行完整保留，总 review bytes 超限失败且不裁剪 | evidence wrong-cell/local-label/1%/invalid AST、renderer adversarial/长行/总预算、semantic audit 已实现 | capability/review/untrusted text 文档已同步 |
| SU-06 Review / freeze / replay | [x] | whole-unit review、只接收真实 HUMAN 选择并从 ReviewUnit 派生 claims 的最小 CLI、Decision 单链；REJECT-empty 与 APPROVE-exact 语义由 append/finalizer/freeze/replay 重放；每个 ReviewUnit 必须有 effective HUMAN decision，Observation roles、consumption 与 Result/Trace exact-complete；workflow/finalizer 不接收 traits；Run 拒绝 fiscal-year/日期矛盾；PASSED/FAILED/NOT_RUN 可 freeze audit，但只有 PASSED 可进 publication；freeze 要求所有 attempt terminal，重放每条 SUCCEEDED response，并拒绝 missing-required-source+PUBLISHED；全 WITHHELD 的 missing-source audit Run 仍可冻结；其余 registry/AI/Observation/Calculator/Result/Trace/receipt 重放保持 | CLI APPROVE/REJECT claims 派生、STARTED、未引用非法 SUCCEEDED response、missing-source PUBLISHED 均失败；missing-source WITHHELD、三种 validation receipt 与既有 trait/graph/period/identity/unit/value/formula/scope/quality/applicability/reason/raw replay/receipt/bytes/socket 正负例已实现 | architecture/interact/SOP/TESTING/capability 已同步；权威 required-source plan 尚未建模，当前先严格消费 manifest 的非空缺失声明；primitive 按单 Run 单写者使用 |
| SU-07 B01 + lodging | [!] | B01/B03 structured、B10/B11/ADR whole-unit Reader与non-lodging N_A正式路径已实现；Marriott真实10-K recorded fixture已到达同一production Reader/Evidence/ReviewUnit path并停在`PENDING_HUMAN_REVIEW`；qualification gate固定要求第二布局receipt→semantic freeze及pre-holdout inventory→独立holdout | unit/identity/N_A/Marriott真实bytes与holdout先后/namespace负例通过；第二真实布局、holdout、live三轮、HUMAN与完整parity未产生 | 缺口仍阻塞正式Cutover，不得把Marriott fixture写成泛化证明 |
| SU-08 Spec-driven B03 | [~] | generic Spec-driven resolver/calculator/Trace与B01 reuse已实现；legacy B03专用resolvers和migrated writers是fail-closed tombstones | 防双计、跨accession、unit/cross-check/Decimal、Marriott/Pfizer anchors及old-resolver-throws公开流程已实现 | 仍缺真实十公司formal staging parity receipt |
| SU-09 Projector / staging / pinned view | [~] | complete Batch/Projector与strict compatibility已实现；每个active terminal cycle只启动一次公开CLI，并以单进程、单次pinned PublicationView transaction贯穿Stage10 Golden、Stage11 report、Stage12 active validation与snapshot publish/verify，root mirrors须等于bundle | exact-set/period/N_A/row/evidence/reconciliation/Decimal、cross-consumer pointer-switch与single-transaction checker回归通过 | 旧producer代码退出完成；真实十公司formal staging与active pointer尚无receipt |
| SU-10 transaction / cutover / rollback | [!] | recorded/formal receipt、immutable bundle、verified legacy A import→formal B、隔离14项fault matrix→receipts→private official CAS顺序、lock/CAS、content-addressed switch intent、hard-crash mirror/edge recovery、committed-predecessor rollback/restore、single-root/single-tip switch graph、latest/active与dynamic active provenance closure已实现；public generic formal receipt/commit入口fail closed | mid-bundle/mirror、pointer写入后crash、pointer-not-commit、initial A→B cleanup/retry、CAS/concurrency/pinned/tamper/WITHHELD、public authority bypass、legacy import terminal cycle、14项fault receipts、legacy-throw与orchestrated new/rollback/restore tests已实现 | 代码/测试完成；当前无active/previous publication，故没有真实Cutover、rollback/restore terminal receipts |
| SU-11 docs / acceptance | [!] | recorded socket-blocked acceptance、full authorization/preflight、固定SEC Stage00/01/02/03/05 acquisition/inventory、portable live audit closure、portable runtime token+binary SHA receipt、Cutover→new/rollback/restore单进程terminal cycles与formal evidence binding已实现；live core exact固定module-owned repository/`artifacts/vnext/cutover`/`outputs`/publication root且fault matrix同样拒绝caller root；每次有效live/HUMAN resume都fresh acquisition后再复用pinned semantic plan；文档同步当前证据等级 | caller-root与preseed自签receipt public/core failure-first、acquisition先于release planning、固定命令、current invocation receipt回绑、当前解释器binary/ledger prefix-tail/attempt/inventory strict receipt重验、无host绝对路径持久化、workspace删除后audit closure重验、NOT_RUN≠PASS、HUMAN blocker、terminal failure propagation、active/mirror不变与binding回归已实现 | 本轮两个secret missing且无qualification/predecessor/HUMAN/live acquisition/full receipts；PR必须Draft并保持`Progress on #12` |

## 2026-08-01 至 2026-08-03 Review / 修复收口记录

| Finding | 历史检测 | 本轮跨入口收口 |
|---|---|---|
| D-01 transport authority | PR #10 已出现“声明权威但实现未执行”的同族问题；本轮独立复审又确认构造后撤回仍可 egress、旧式无 observation result 会补造获批 host、caller duck adapter 可伪装 recorded no-egress，且合法 adapter 的 `_transport` 可被替换 | 仓库 builder 只构造私有 exact adapter 并在 `complete` 前验证 factory authority/固定类实现；approved adapter 不保留 transport，每次 outbound 从模块 registry 新建并校验；每次重验当前 policy/closure；attempt 只记录实际 observation；异常/旧式结果缺 observation 不猜测；freeze/replay 重验 SUCCEEDED attempt |
| ReviewDecision 语义 | PR #13 R8 已关闭 ReviewUnit 入口但明确留下 Decision 自身入口；R15 已要求按不变量跨入口收口 | `REJECT-empty` 进入 record validator；`APPROVE-exact` 进入 binding validator；append、全 supersedes chain、finalizer、freeze/load/replay 共用 |
| table-grid / renderer 资源 | PR #13 R13 声称覆盖性能但未产生资源测试；本轮独立复审又确认 Python 3.9 会对超长 span/entity 十进制词法做近二次方大整数解析 | 集中预算覆盖 span/entity 词法、source/expanded cells、维度与文字；大整数转换及物化前失败；review 无损分行与总 bytes fail closed |
| freeze SOP 状态 | PR #9 已出现文档漂移同族问题 | SOP 使用结构化三状态表，并由 freeze/publication 场景测试与代码常量联合验收 |
| Run terminal/source authority | PR #13 R6/R8 的相邻对象绑定曾仅部分修复 | freeze/load 拒绝 STARTED、重放全部 SUCCEEDED response；missing role 与 PUBLISHED 不可并存，WITHHELD audit 保留；权威 required-source plan 仍诚实列为后续项 |
| Projector / publisher 双真相 | PR #13 R1/R2/R9/R15/R32/R33 及 R37 多轮出现 caller 自洽 bytes、receipt、空 audit artifact 或局部 exact set 未绑定完整业务事实的同族问题 | content-addressed BatchManifest 绑定完整 PASSED FROZEN Run 集合；frozen baseline 绑定 legacy metrics/evidence/Golden exact bytes；Projector 生成 candidate 与 repair rows并保存 result-row proof；publisher 重跑 Projector 与真实 semantic/scalability gates，独立绑定 checker/dependency bytes，拒绝任意 legacy/staging、自写 repair、自签 PASS、伪造空 audit 与替换 checker replay |
| D-01 workflow 第二 root | R15/R39/R40 依次关闭 caller policy/root、duck adapter 与可替换 transport，但 approved adapter 的固定 D-01 root 仍可与 `create_review_run(repo_root=...)` 的 payload root 自由组合 | 不新增 caller closure/hash/DTO；approved workflow 在读取 Spec/filing bytes 前要求 payload root 与模块 D-01 authority 解析为同一物理 repository，A-root adapter + B-root workflow 负例断言 transport calls=0；recorded fixture 仍可使用隔离 root |
| Publication ledger 权威与最小前缀 | R16 只把 caller mapping 纳入 receipt hash，R32/R33/R35/R37 又多次暴露“调用方复述系统事实”的同族问题；删除 caller mapping 后复审又确认整表 hash 会让未使用的合法 tail append 阻断同一 candidate，row 的声明 locator 还可能指向不存在的文件 | 删除 runner/prepare 的 caller `ledger_binding`；从 verified Batch 的 Observation/实际 AI attempt 消费路径派生 SourceReference exact set，以 current-schema row+位置重算 SEC attempt ID；先验证整表 manifest，再绑定截至最后一个已用 row 的原始 CSV prefix，并要求 row 声明 locator 与 immutable attempt 同一。failure-first 场景分别证明 tail append 曾使 receipt stale、假 locator 曾被接受；修复后前者保持 view、后者在 PASS/PUBLISHABLE 前失败 |
| Projector Decimal context | PR #13 R41 发现 projection multiplier 与 Golden tolerance comparison 继承调用进程可变的全局 Decimal precision，可能改变 candidate bytes 或临界 verdict | 两处实际 Projector 运算复用 canonical 28/ROUND_HALF_EVEN context；同一测试在低精度外部 context 下同时锁定完整 projection digits 与 tolerance 边界 |
| Batch company×metric 完整性 | R33 修复 metric 子集后仍只检查单公司 Run 的 metric-name set，未建立 release company 维度 | expected set 从 registry、traits/applicability 与 release plan 派生；baseline 提供 legacy exact bytes/parity；缺公司、缺 N/A、重复/额外坐标与跨 period 混批失败 |
| non-lodging N/A 持久化 | workflow 的 trait 短路早于 `create_run`，现有测试只看返回字典，导致 Calculator 已有 N/A 分支无法进入 freeze/replay/project | source/AI 仍为零，但先从仓库 Spec 构造 Result/Trace，再创建 Run 并持久化；场景测试覆盖 Run 目录、exact records、freeze 与 replay |
| Publication 声明真实性 | Projector helpers 仅测试调用、任意 staging/自报 PASS 可 publish 时，architecture/TESTING/capability 仍写成 active verified release | tracked architecture/interact/TESTING/capability/SOP/本清单按真实 batch/gate/read-back 边界同步；远端 PR body 与 thread ledger 只在明确发布授权后更新 |
| Publication 路径与 latest 自报 | PR #13 R16/R17/R25 逐次修补 caller-supplied path/view | commit/rollback/recovery/view/status 统一从 publication root 派生；latest 只从 persisted Run/publication locator 推导状态与 boolean |
| SEC identity / canary | live 前置曾只排除少数 example email；acceptance 随机 token 未进入 producer，也未写入待扫描文件 | acceptance 与 SecHttpClient 共用非空/格式/example validator；acceptance 只证明本次 publishable-root traversal 执行，token 阳性检出与 symlink fail-closed 分别由独立单测证明，真实 producer 闭环留作 live 前硬门 |

本节记录本地实现事实；发布前把本表的新收口项与最终动态 receipt ID 同步到 ignored `PR_BODY.md` 的 Review / 修复记录。receipt ID 不写回本 tracked 状态表，避免验收回执与 source bytes 循环漂移。

## 外部 gate

- D-01 已由 exact R3 Addendum 的 superseding record批准；immutable R2中的pending事实继续可追溯。不得把“历史仍存在pending record”误读为effective decision仍pending。
- 本轮环境缺少`OPENAI_API_KEY`与`SEC_CONTACT_EMAIL`；SEC organization固定`axaxl`，email不得写入配置或伪造。没有secret就没有remote live三轮或SEC full evidence。
- Marriott真实recorded fixture已到达同一production Reader/Evidence/ReviewUnit path并停在`PENDING_HUMAN_REVIEW`；第二个materially different lodging fixture仍未形成receipt，因此production freeze/pre-holdout inventory也尚未产生，独立holdout不能提前加入或伪造。
- fixed live SEC Stage00/01/02/03/05 acquisition/inventory、legacy migrated writer/resolver fail closed与Stage10/11/12 PublicationView分支均已实现；但本轮因secrets/qualification/HUMAN缺失未执行live acquisition且无receipt，也没有十公司formal staging、committed active/previous publication、真实rollback/restore terminal cycles或full receipt。
- 本 PR 只能保持Draft并写`Progress on #12`。不得把实现测试、recorded receipt或合并动作解释为live/full/Cutover成功，也不得写`Closes #12`。
